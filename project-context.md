# Contexte Projet — TradingApp v3

> Lu par tous les agents avant toute action.
> **Repo privé** (données financières personnelles). Dernière mise à jour : 2026-06-19 (Session 8).
> **Stade : V1 EN COURS** (mode shadow — rien émis publiquement ; go-live cible **08/08**). Reste avant V1 (max 3) : (1) **~30 j shadow forward** → WR ≥ 70 % / N ≥ 15 par cellule (jalon 08/08) ; (2) **trancher les 3 cutovers sur preuve** (persistance / rumeur→verbal / garde-fous démenti-déjà-coté) après ~2 sem. de mesure shadow ; (3) **régénérer la clé Twelve**. [Question produit « rôle des news » **TRANCHÉE 20/06 (S9)** : quant reste dominant, `NEWS_DOMINANT_RATIO` inchangé — cf. Mémo.] **Validation = SHADOW RÉEL uniquement** (décision fondateur 05/06 : le backtest n'est PAS un jalon).
> **Source de vérité produit complète : vault Drive `Bourse/Bourse.md`** (ce fichier en est le résumé technique).
> **🌐 PAGE LIVE (consultée par Thomas tous les jours) : https://trading.issa-capital.com/** — c'est l'interface réelle du produit. Servie via le **VPS Anya** (déploiement hors-repo ; outils MCP `VPS - débogage` : `etat_services`, `journal_deploiement`, `forcer_deploiement`). Le repo ne contient AUCUN workflow Pages/deploy : pousser `v3/data/index.html` sur `main` ne suffit pas forcément à mettre la page à jour → vérifier/forcer le déploiement VPS. Le cron `cycle.yml` régénère `index.html` à chaque run ; le VPS le sert.

## En une phrase

Système qui produit **3 fois par jour (7h/12h/18h CET)** un **bulletin de positionnement directionnel** sur **12 actifs × 3 horizons (24h/7j/1m)** : chaque cellule tranche **LONG ou SHORT**. Thomas (trader humain) lit le bulletin et exécute manuellement des turbos chez Bourse Direct. Le système ne place jamais d'ordre.

## Objectif (NON NÉGOCIABLE)

⚠️ **On ne cherche PAS l'edge à la minute.** Un retard de 4h sur une news n'est pas grave.
But = **être certain de la TENDANCE par actif**, pour **suivre les vagues de hausse/baisse dans le bon sens, sans erreur de direction**. C'est du **trend-following directionnel**, pas du front-running de news. Tout raisonnement qui critique la *vitesse de capture* est hors-sujet.

## Architecture (30/05/2026)

- **GitHub Actions + git-as-storage** — scripts Python, **sans VPS, sans Cowork, sans Drive**. Coût infra 0 €.
- Workflow unique `cycle-decision` (cron 3×/jour) enchaîne : **ingest** (26 sources + extraction DeepSeek) → **critères** (Twelve Data/CFTC/EIA/Open-Meteo + triplets news) → **bulletin** (scoring + Briefing) → **mesure** (Journaliste). Commit git atomique.
- Tout le système est dans **`v3/`**. Données et historiques commités dans `v3/data/`.

## Méthode

- Score pondéré de critères par actif × horizon → LONG si >0, SHORT sinon (jamais neutre).
- 2 types de critères : **numériques** (prix, z-scores, COT, météo — cœur de la tendance) + **événementiels** (news, direction donnée par DeepSeek = contexte).
- **A/B** : score **±1** (référence) + score **pondéré** (matérialité×fiabilité) calculés en parallèle, mesurés ; activation du pondéré sur preuve.
- **Observabilité** : `decision-log` JSONL par cycle (tout tracé, historique git requêtable).

## KPI

Taux de réussite > **70 %** + Brier < 0,25 par cellule (30 dernières conclusions). Mesure A/B dans `v3/data/performance-ab.md`. Kill criterion : `v3/KILL-CRITERION.md`.

## État (01/06/2026)

- ✅ Système v3 en **mode shadow** (cron 3×/jour, rien émis). **619 tests** verts.
- ✅ Pipeline complet sur vraies données : **indices via ETF Twelve** (SPY/QQQ/FCHI/VIXY… — yfinance bloqué sur les runners CI), métaux/commodities Twelve, taux FRED, vol CBOE, news DeepSeek. 0 tie-break, ~117 critères/run.
- ✅ **Boucle prédiction→mesure fermée** : prix d'émission stampés, conclusions VRAI/FAUX (Journaliste), KPI Wilson/Brier en warm-up.
- ✅ Session 01/06 (cf. Historique) : synthèse directionnelle 2 niveaux + contexte-prix, rate-limiter Twelve (attend, RPM=55), PROBA_SCALE=15, **plan horizon** (pertinence recalibrée + cap anti-inversion news α=0.8 + drapeau 📰 si news>50%).
- ⚠️ **GitHub `schedule` retardé de 1-3h** (comportement GitHub normal, PAS un bug — 6 runs schedule prouvés les 30-31/05). Parade : redondance ×3 par créneau (cron `:12/:27/:42`) + garde-fou anti-doublon (skip si snapshot <2h). Fallback fiables : push `v3/RUN-CYCLE.txt` ou `workflow_dispatch`. **Ne plus churner le cron** (re-registration fait sauter l'occurrence suivante). **→ RÉSOLU 02/06 : pinger sur le VPS Anya (`v3/ops/vps-trigger/`) déclenche `workflow_dispatch` 3×/jour à l'heure pile ; schedule GitHub gardé en fallback.**
- ✅ Session 02/06 (cf. Historique) : **gate S5 suffisance de données** (couverture pondérée + paliers `⚠️ conf. faible`/`🚫 insuffisant`, abstention override jamais-neutre), **6 lots de gates anti-erreur de jugement** (intégrité quant z-score/std0/spike + réconciliation Σ + UTC, signe DeepSeek C1, intégrité mesure C5, détecteurs directionnels flag-only, sanity news déjà-coté/démenti, publication « cellules à surveiller » + flip/continuation), **source_monitor** (santé flux/cycle + 4 flux muets réparés), **requêtes news 10/10** (concertation 3 experts ×3 rounds), backtest quant v1 (NO-GO partiel sur price-only), bilan news, heure dans titre bulletin.
- ✅ **RÉSOLU (priorité 1) — 02/06** : la couverture basse (5 actifs `INSUFFISANT` sur run frais 12h29, 42% du bulletin) n'était **pas** un seuil mal calibré ni un trou d'archi, mais une **panne FRED 429** (rate-limit → `DFII10`/TIPS + spreads morts, prouvé dans les logs du run). `COVERAGE_MIN=0.40` reste juste. Corrigé : retry/backoff + throttle FRED (commit `a03cd98`, +6 tests). Diagnostic via audit trio du bulletin (AJUSTER 6/10 → `v3/audit/coherence-bulletin-2026-06-02.md`).
- ✅ **Fait S4 (05-09/06)** : **refonte « 5 rapports/jour »** live (mesure ouverture→clôture, 1 décision notée/jour, Briefing 7h/Suivis 12h-18h/Bilan 22h15/Bilan semaine+**Manager**), VPS redéployé, page affiche tous les rapports + tableau win-rate. Backtest v2 = NO-GO **écarté comme jalon** (validation = shadow réel). mining_com retiré, garde fériés+week-end généralisée. Bulletin reformulé langage trader.
- ⬜ **À venir** (détail dans Mémo de reprise) : laisser tourner la nouvelle structure en shadow → **calage ~23/06** (ticket C / le Manager : frein contre-momentum CAC, pertinence S&P 7j, échelles) → **Phase 2 news (`nature`)** au service de la tendance → ~30 j shadow KPI>70% → émission post-shadow. Q5 (close CAC) à vérifier en live.
- ✅ **Fait S3 (03/06)** : garde week-end + échéance jour-ouvré/fériés, verrou look-ahead **armé**, dédup news **inter-jours**, **données comblées** (DXY, taux 10Y delta, spread OAT-Bund, Caixin, Shiller CAPE, différentiel 2Y US-DE p12), refonte page (dark mode, onglet Historique, favicon).
- ⬜ **#3 — Combler les critères absents à fort poids** (chantier sources, issu de l'audit trio du bulletin 02/06) : Caixin PMI Chine (cuivre p.12), différentiel taux 2Y US-DE (EUR/USD p.12), delta taux 5j + AAII + CAPE (S&P), DXY, FedWatch… Aujourd'hui plusieurs directions reposent sur les critères secondaires. Nécessite de nouvelles intégrations de données (pas un simple fix code). Couverture partiellement récupérée le 02/06 via fix normalisation composite/mapping (+VIX/V2X/VXN régimes, café météo, cacao HF).

## Garde-fous

- **Zéro invention** (source absente → critère n/a, poids 0). **Pas de modif silencieuse** des poids/prompts (changelog + validation Thomas). **Mode shadow obligatoire** avant toute émission. **Échec visible**.

## Repères repo

- `v3/scripts/` : le système. `v3/config/` : fiches actifs (YAML) + triggers + weighting. `v3/data/` : sorties (bulletins, events-log, decision-log, performance). `v3/audit/` : audits. `v3/docs/reco/` : décisions d'architecture.
- `legacy/` : ancienne app (bot « 1 signal turbo/jour ») — **archivée, ne pas utiliser**.
- `.claude/`, `CLAUDE.md`, `update.sh` : framework Gradient Agents (séparé du projet trading).

## Historique des interventions agents

| Date | Agent | Livrable | Décisions clés |
|---|---|---|---|
| 2026-07-01 | orchestration + @fullstack (×3) (S11) | **Implémentation intégrale de l'audit bulletin (24 points, GO fondateur)** : règles de sélection + ticket C + capteurs + 17 corrections de rendu (cf. CHANGELOG 01/07) | **Sélection (zéro cutover)** : véto « news contre tendance 3j » (cas Cacao 01/07), plancher `SELECTION_INTENSITE_MIN=0.30` (calibré decision-logs 23/06→01/07), conviction plafonnée « fragile (capteurs éteints) » (poids max n/a ou ≥2 poids ≥8 n/a, cas Blé). **Ticket C** : le reweight 30/06 avait déjà tout mis à 0.1 sauf `usd_jpy_proxy_risk` (EUR/USD) 0.7→0.1 ⇒ EUR/USD 24h SHORT→LONG ⇒ **cutover EUR=X 02/07 seul** (CAC conforme, preuve en addendum). Signe météo cacao : conforme S8, mention « à valider » retirée. **🐛 PMI Caixin** : placeholder hors-fenêtre émettait 0.0 dégénéré → n/a propre (aucun impact score) ; V2X/VXN = yfinance-only bloqué CI, absence assumée. **Rendu (17 pts)** : raisons = titres réels (fin « pas de titre représentatif »), Porté-par jamais à contre-sens, Note≠Conviction, « forte » = intensité ≥0.30, surveillance = Sélection+flips, table Intensité supprimée (décision fondateur : Synthèse = table centrale), légende complète + test anti-symbole-orphelin, colonne Objectif 24h, « (écarté des paris) », formes humaines, zéro cadratin dans les noms. **Incidents S11** : redémarrage conteneur (1 agent perdu, relancé) ; des tests polluent `v3/data/` au run (nettoyé avant chaque commit, dette à isoler). Effectif au bulletin du 02/07. Mergé sur `main` (autorisation fondateur, chat 01/07). |
| 2026-07-01 | trio Analyst/NewsTrader/Spéculateur + orchestration (S11) | **Audit complet fond+forme vs objectifs gains 24h/7j/1m** (`v3/audit/audit-fond-analyst-2026-07-01.md` 4/10, `audit-fond-newstrader-2026-07-01.md` 6,5/10, `audit-forme-speculateur-2026-07-01.md` 6,5·3,5·5/10, synthèse `coherence-audit-2026-07-01.md`) | **Verdicts horizon** : 24h seul en course au 08/08 (N~26-28 si zéro reset ; WR conclusif 72 % MAIS WR tradable ~43 % : plafond 37 % de non-conclusives rend la règle gravée inatteignable en l'état) ; **7j impossible au 08/08** (max ~5 obs, jalon honnête ~13/10) ; **1m impossible** (0 mesure, 1re comptée ~30/07). « N=1 affiché » élucidé : **6e reset 30/06** (commit `bac8ccb`, re-découpage horizons, session non tracée ici) écarte 87/102 mesures, pas un bug. **P0 signal** : Nasdaq 5 FAUSSE news-driven SHORT consécutives (news a renversé la tendance ; hors Nasdaq news-driven = 20/22) → replay contrefactuel avant retouche. **Cutovers news : 3× NO-GO** (persistance : événement discriminant jamais survenu ; rumeur : contrefactuel non loggé ; démenti textuel : NO-GO DÉFINITIF, le quant est le démenti, L028). **Vérifié racine** : `futures-us/` absent (secret OANDA jamais posé) → suivi US aveugle à midi ; sucre/usdjpy 4/4 suivi-interrompu (mortes-nées). Décisions Thomas requises : jalons par horizon, seuils NC puis moratoire resets, secret OANDA. |
| 2026-06-26 | @fullstack (S10) | **Implémentation 3 nouveaux actifs (USD/JPY, Coton, Sucre) + fix routage news CAC40** | **P0 fix CAC40** : `cac_40` → `cac40` partout (TICKER_TO_ACTIF, IA_ASSET_TO_ACTIF, CRITERION_SCOPE, triggers-and-windows.yml) — les news CAC routent désormais sur la fiche `cac40` (T1 audit news résolu). Test `test_cac40_news_routing.py`. **3 fiches** `config/fiches/{usdjpy,coton,sucre}.yml` (8 crit + gate chacune, gate partagé `gate_regime_extreme` aligné sur les 12 existantes). **Câblage criteres_calculator.py** : CFTC_MARKETS (3 noms EXACTS vérifiés sur Socrata : JAPANESE YEN - CME / COTTON NO. 2 - ICE FUTURES U.S. / SUGAR NO. 11 - ICE FUTURES U.S., séries live confirmées) ; METEO_CRITERIA (Lubbock TX / Gujarat / Ribeirão Preto) ; FRED_SPREADS (`diff_taux_10y_us_jp` + proxy `diff_taux_2y_us_jp` — **série JP corrigée `IRLTLT01JPM156N`, l'audit citait `IRGTLT01JPM156N` qui est un 404 FRED**) ; TWELVE_SYMBOLS (momentum 7j/20j usdjpy via USDJPY=X, coton via COTN, sucre via CANE + nouvelles clés `brent_ethanol_proxy_sucre`/`usd_brl_sucre`/`vix_risk_usdjpy`, L023) ; market_data._TICKER_MAP (COTN/CANE passthrough Twelve). Critères sans source (USDA CropProgress coton sans clé, taux BoJ daily, ESALQ) = n/a propre / triplet news, **zéro invention**. **Cutover** : 3 entrées `ref-changed.json` (ref_changed 26/06, warm-up zéro, PAS un reset des 12 existants) + addendums KILL-CRITERION.md/SELECTION-RULE.md (univers 12→15). **News (Volet B audit)** : keywords + IA_ASSET_TO_ACTIF + 4 triplets triggers-and-windows.yml + 3 flux dédiés `gnews_{usdjpy,sugar,cotton}` (config.py) + extractor TRADABLE_ASSETS (USDJPY/COTTON/SUGAR). **Tests** : 15 actifs enregistrés, score LONG/SHORT cohérent au signe, n/a propre sans donnée (`test_nouveaux_actifs_2026_06_26.py`). Suite : 1847 passed, **6 FAILED pré-existants** (4 holidays/env + 2 wording news-label, prouvés sur baseline stashée) **zéro régression de mon fait** ; 3 tests de comptage mis à jour (cutover 12→15, momentum 11→14, wording NASS retiré du nom coton). NE PAS COMMITTÉ (Thomas revoit + déploie). |
| 2026-06-26 | @data-analyst (S10) | **Fiches-drafts 3 nouveaux actifs** (`v3/audit/nouveaux-actifs-fiches-2026-06-25.md`) | Panel 3 experts (A/NT/S) · 3 désaccords résolus : (1) poids momentum vs fondamentaux USD/JPY → diff 2Y poids 11, momentum 7j poids 6 ; (2) parité éthanol sucre → proxy Brent XBR/USD poids 7 (conservé, source documentée) ; (3) météo coton Texas only vs Texas+Inde → deux critères `zscore_abs` (Texas poids 9, Gujarat poids 5). USD/JPY : 8 critères (FRED DGS2−BOJ proxy, DXY, momentum 7j/20j Twelve, COT JPY, spread 10Y, VIX, triplet BoJ) — note 8/10. Cotton COTN : 9 critères (météo TX+IN, COT ICE, momentum 7j/20j, DXY, triplet Chine, USDA CropProgress ⚠️ manuel) — note 7/10. Sucre CANE : 9 critères (météo BR, proxy Brent éthanol, USD/BRL, COT ICE, momentum 7j/20j, triplets Inde/Thaïlande + exports Brésil) — note 7/10. Plan câblage : 3 ajouts CFTC_MARKETS, 3 ajouts METEO_CRITERIA, 1 ajout FRED_SPREADS, dates BoJ 2026. Clés toutes uniques (L023 respecté). NE PAS créer les .yml config/ sans GO Thomas. |
| 2026-06-25 | @design (S10) | **Identité visuelle page publique** (`v3/docs/design/identite-page-publique-s10.md`) | Direction : « Clarté de conviction » (sérieux financier, sobriété, confiance). 16 nouveaux tokens CSS (brand-primary #1a2e4a, brand-gold #c9a84c, hero-bg, footer-bg, validation-bg/border/text). Header refondu : fond bleu nuit, hauteur 64px/52px, wordmark `Issa Capital` + tagline + badge validation pill ambre pulse lent. Hero : section `page-hero` fond #0d1f35, 3 stats factuelles (12 actifs, 3 bulletins/j, win rate en chauffe). Footer : après `.layout`, fond #0d1826, disclaimer WIN RATE ONLY. Badge validation : `.validation-badge` pill ambre désaturé, pulse 2.4s, texte `Validation · 08/08`. Favicon SVG triangle outline doré sur fond bleu nuit. Tous les snippets CSS+HTML prêts à coller dans `build_html.py`. WCAG AA vérifié sur tous les nouveaux tokens. |
| 2026-06-25 | @ux (S10) | **Spec refonte page publique pour primo-visiteur** (`v3/docs/ux/refonte-page-publique-s10.md`) | Cible : primo-visiteur non-trader. Problème : page fait "très technique", incompréhensible en 10 sec. 5 livrables : (1) parcours de lecture en 4 zones (hero → preuve → nav → footer) ; (2) spec hero 5 blocs (accroche, sous-titre, statut validation honnête, ce qu'on ne fait pas, CTA) ; (3) refonte sidebar 3 groupes libellés humains (Résultats / Bulletins / Marchés) + suppression "Sauter à :" ; (4) spec footer 3 colonnes (identité, statut, disclaimer) ; (5) 8 instructions @fullstack P0/P1/P2 avec références ligne exactes dans `build_html.py`. Contraintes respectées : WIN RATE ONLY, statut "en validation" visible et sobre, zéro tiret cadratin, ancres `#vue=...` inchangées, parsers markdown intacts. Handoff : @copywriter (textes hero + tagline) + @fullstack (implémentation). |
| 2026-06-23 | orchestration + @fullstack + @infrastructure | **Données US réparées : bug stamp 18h corrigé + futures US via OANDA (sans VPS)** | **Incident « infichu d'avoir les données US à 18h »** : PAS un problème Twelve (le cash US S&P/Nasdaq est servi en séance). Cause = **timing du stamp** : le cash US ouvre 15h30 (+5 min = 15h35 stampable), or le créneau stamp tire à :12/:27/:42 ; seul :42 (15h42) est après l'ouverture, mais l'anti-doublon (2h) le suspendait à cause du stamp :12 d'AVANT l'ouverture → référence d'ouverture US jamais captée. **Fix** : créneaux stamp (Paris 8h/9h/15h) exemptés d'anti-doublon (idempotent + entry-lock) ; « stamp ouverture » retiré du grep anti-doublon. **Sonde 23/06 (clé GROW réelle)** : Twelve ne sert AUCUN US overnight (ES/NQ vides/figés, CFD US500/USTEC/US30 vides, SPY/QQQ prepost vides). **Thomas trade en TURBO** (suit le future, revente possible à midi) → besoin du future dès 8h. **Décision Thomas : API courtier OANDA (démo gratuite, sans VPS)** — `fetch_us_index.py` lit `SPX500_USD/NAS100_USD/US30_USD` (CFD ~24h/5) DANS le cycle, écrit `futures-us/{date}.json` (1ère cotation 7h = entrée turbo), run_suivi affiche « 🔵 via future » dès 12h (mouvement indice vs indice, échelle cohérente). **Piste VPS/yfinance ABANDONNÉE** (Yahoo bloque les IP GitHub ; install fragile). **Reste (P1) : secret `OANDA_API_TOKEN` à ajouter par Thomas** ; ensuite mesurer le bilan US depuis l'entrée 8h (OANDA) et non l'ouverture cash 15h30 (cohérence turbo, à câbler). |
| 2026-06-20 | orchestration + @ux + @design + @fullstack (S9) | **Refonte page HTML en 3 vagues (header épuré, légende unique, nav lisible, zéro `—` affiché, regroupement « Décision du jour », responsive PC/mobile)** | Demande Thomas : interface agréable/simple surtout le header ; infos répétées ; « Sauter à » imbittable ; **zéro tiret cadratin** (« fait très IA ») ; itérer 10/10 PC ET mobile. **Vague 1** (`517214e`) : header 1 ligne 44px + badge « Mode test » discret (point ambre, context-banner absorbé), **légende symboles place canonique UNIQUE = help-box** (retirée du header, legend-bar réduite), **« Sauter à » réparé** (mots lisibles, fini les 3 émojis 🎯 nus, n'affiche que les sections ouvertes), tokens dark. **Vague 2** (`2f79ab6`) : **retrait des `—` dans toutes les chaînes affichées** (build_html ~14, scoring_analyste ~30, briefing 1, raisons-drivers.yml 77 ; placeholders n/a + commentaires conservés) → ponctuation FR naturelle ; compteur « (N flips) » ; **responsive PC ET mobile** (subnav flex-wrap, tables `overflow-x:auto`, `overflow-wrap`, scroll-margins). **Vague 3** (`33f9fe7`, validée Thomas) : fusion des blocs 24h en UN seul **`## 🎯 Décision du jour`** (encart `### Sélection (max 3)` carte `.decision-selection` + sous-bloc `### À jouer`), **alertes remontées** (Cellules à surveiller + Drivers juste après Décision, avant le panorama), echo bilan_jour réaligné. Structure markdown pure (parser journaliste/briefing intacts). **Tests : 1474 passed / 22 env-only à chaque vague (vérifié L024 moi-même 3×, zéro régression** — un sous-agent avait mal compté « 1364 »). Visuel validé côté CODE (pas de navigateur headless) + aperçu envoyé à Thomas ; le regroupement « Décision du jour » et le `—`-cleanup du CORPS n'apparaîtront que sur le bulletin frais (run live **lundi 23/06**). `v3/data/` non pollué. |
| 2026-06-20 | @ux (S9) | **Audit architecture d'information page HTML** (`docs/ux/audit-page-s9.md`) | Notes : Comprendre 4/10 · Décider 6/10 · Juger 5/10. **4 répétitions documentées** avec décision canonique : (1) légende symboles → help-box uniquement, supprimer ligne header L880-888, réduire legend-bar ; (2) 4 vues 24h → fusionner Sélection+À jouer en "Décision du jour" ; (3) "Comment lire les scores" dans le markdown → supprimer (doublon exact de help-box) ; (4) bandeau OK tel quel. **Subnav corrigée** : quick-win I1 (L1509 JS, retirer émojis en tête avant split). **Header réduit à 1 ligne** : titre bulletin + mode test. **5 sections à replier** : Santé sources / Intensité / News (si vide) / Flips / Détail. **14 instructions @fullstack ordonnées P0/P1/P2**. Décision Thomas requise avant I13 : ordre Synthèse vs Décision du jour. |
| 2026-06-20 | orchestration (reprise S9) + @infrastructure | **Reprise S9 : vérif santé post-Juneteenth (sur pièces) + question « rôle des news » TRANCHÉE + retrait scaffolding sonde 8h** | **P1 — santé sur pièces (GitHub MCP)** : tous les runs 19/06 verts (matin forcé 05:15→07:15 puis schedule repris 09:51→22:28) ; fix incident `is_any_market_open` confirmé (le cycle a tourné au lieu de NO-OP total) ; le « 0 à impact » du briefing **08h08** = **pré-fix attendu** (briefing commité 06:11 UTC, 36 min AVANT le fix parser `9211ce1` à 06:47) — fix L029 bien sur main, rendu news re-confirmé sur le suivi 18h post-fix (« 3 actus ») ; 20/06 NO-OP = normal (samedi). Reconfirmation finale du briefing complet post-fix = **lundi 23/06**. **P3 — question produit « rôle des news » TRANCHÉE** (Thomas suit la reco des 3 experts) : le **quant reste dominant**, `NEWS_DOMINANT_RATIO` **inchangé** ; piste = IA news plus maligne + garde-fous démenti/déjà-coté **après mesure** (3 instruments shadow) ; **zéro changement de signal** aujourd'hui (« mesurer avant d'agir »). Gravé project-context + founder-preferences. **P2 — retrait du scaffolding cron « sonde 8h »** (verdict B rendu S7 : ES=F/NQ=F/VX=F absents Twelve free) : `cycle.yml` (step relais + routage `probe8h` + permission `actions:write` retirés) + `probe-futures-8h.yml` (cron temporaire supprimé, `workflow_dispatch` manuel conservé) ; slot 08h05 continus intact ; YAML OK + `test-guard-logic.sh` **ALL PASS (30)** + `test_briefing` 14 verts ; 0 résidu `probe8h`. **L014 clos** (hors-TTL, résolu dès S2 par l'incident FRED-429). |
| 2026-06-16/17 | orchestration + @fullstack (S8) | **GO mesure L027 + interprétation honnête cacao + intros rapports + fix double-comptage news + libellé dynamique** | **Thomas a donné le « GO mesure »** → fix **P0** de la fenêtre de mesure des continus : réf 24h = **émission 7h** (point d'exécution réel, L021/L027), plus l'« ouverture » 8h arbitraire qui tombait au milieu d'un mouvement et **cachait des pertes** (short or 15/06 classé NC → désormais **FAUX**, test-verrou). **Option bougie open→close écartée** (open de bougie = frontière de session ≠ 7h quand Thomas agit → recrée le bug). Cutover 8 continus au 17/06 (4e reset assumé, vrai défaut). + cacao interprétation honnête + sources offre + capteur choc-d'offre shadow. + **intros « décor du jour »** (7h) + actus fraîches (suivis). + **fix double-comptage** de la synthèse nette (cuivre/pétrole/cacao, L013). + **libellé dynamique** des créneaux news (honnête selon mode net/keyword). |
| 2026-06-18 | orchestration + @fullstack + 3 experts (S8) | **Refonte briefing 7h (9 pts) + « raison par cellule » 10/10 + 3 instrumentations mesure (persistance / news / rumeur) + note normalisée** | **Refonte briefing 9 points** (pur rendu) : décor en intro, légende consolidée « Comment lire », « Porté par » détaillé, **News par actif**, synthèse dépliée, **fix prix de réf. vides** (backfill live — bug d'affichage, PAS la clé Twelve). **« Raison principale par cellule »** (Synthèse 24h/7j/1m) : driver dominant **DANS LE SENS** de la conclusion (jamais le \|effet\| brut à contre-sens), via biblio experte éditable `raisons-drivers.yml`, **amorcée par audit marché des 3 experts puis itérée jusqu'à 10/10** (3 rounds). **Note normalisée** comparable inter-actifs (la note brute = somme non comparable — Analyst). **3 instrumentations SHADOW** (mesurer avant cutover, consensus 3 experts) : (a) persistance — le vrai « démenti » = le **quant qui se retourne**, pas un mot-clé (L028 ; ~71% des structurels 7j/1m déjà démentis par le quant) ; (b) justesse news suivie à part du quant **et par horizon** (le tag news_driven n'était jamais sauvé) ; (c) détecteur **rumeur→verbal** (59,9% des « structurel » non-confirmés, activation graduée). Zéro impact signal. |
| 2026-06-18 | orchestration + @fullstack + panel 3 experts (S8) | **5 critères météo rendus symétriques (sécheresse OU excès = haussier) — bug de signe** | Bug confirmé sur le code : les fiches déclaraient une intention SYMÉTRIQUE mais l'impl. `zscore`+signe linéaire rendait la **sécheresse baissière** (à contre-sens). Nouveau mode `normalisation: zscore_abs` (\|z\|). Appliqué à **cacao, café-Brésil, blé-US, café-Vietnam** ; **blé-Australie laissé DIRECTIONNEL** (verdict panel 3 experts, sourcé Bloomberg/Reuters : culture sèche mono-risque sécheresse → la symétrie créerait un faux signal haussier les saisons humides, zéro invention). **Zéro cutover** (valeurs du jour z>0 → direction inchangée, seul le côté sécheresse est réparé). Dimension **gel café** signalée = chantier data séparé (non inventée). cle_courante/poids inchangés (L023). |
| 2026-06-19 | orchestration + @fullstack (S8) | **Incident « pas de rapport 7h » (Juneteenth) : force + garde-fou ≥1 marché ouvert + fix parser news + bandeau compact** | **Diagnostic prod sur pièces (GitHub MCP)** : 19/06 = Juneteenth (NYSE fermé) → garde-fou `is_trading_day` (NO-OP dès qu'UN marché fermé) a sauté **tout** le cycle, alors que CAC + continus cotent. **Forcé** (`workflow_dispatch force=true`) → briefing publié (label 08h, ingestion 9 min). **Corrigé** : `is_any_market_open(d)` (tourne si ≥1 marché ouvert ; NO-OP total seulement week-end/férié global) + statut `marche-ferme` (actifs à marché fermé non jugés → zéro mesure 24h dégénérée). Règle les ~8 fériés US/an. **Fix parser news** : le briefing droppait toutes les lignes events-log au format courant (19 cols — materiality n'est plus en fin de ligne) → « 0 à impact / aucune actualité » partout (Décor + News par actif). Parsing par **index nommé**, robuste aux futures colonnes : **0 → 161 events à impact**. (Scoring NON touché — parser séparé `triggers_classifier`.) **Bandeau** de tête compacté (3 lignes → 1). 1508 tests. |
| 2026-06-16 | orchestration + @fullstack | **Twelve = vraie source (or/argent/Brent) + bug de mesure or découvert + gate asset-aware** | **Vérif sur pièces avec la clé Twelve réelle** (vault `_Cles-API.md`). (1) **Twelve ne sert PAS l'historique des futures** (GC=F/SI=F/BZ=F/HG=F/CC=F/KC=F/ZW=F = 404 sur /time_series) → le système dépendait d'un **fallback yfinance caché** ; les fiches disaient « Twelve » à tort. (2) **Remap or→XAU/USD, argent→XAG/USD, Brent→XBR/USD** (natifs Twelve, niveaux vérifiés ✓), prix+historique, ticker_principal inchangé (L023), provenance loggée (twelve_native/yfinance_fallback/stooq) dans criteres-health. **Cuivre/cacao/café/blé : Twelve ne les sert pas → restent yfinance, libellé honnête** (pas d'ETF au mauvais niveau = zéro invention). Cutover ref_changed GC/SI/BZ au 16/06. (3) **BUG DE MESURE confirmé via XAU/USD** : le 15/06 l'or a fait open 4215→close 4309 (**+2,2 %**, plus haut +3,6 %) — le SHORT a perdu — mais le système l'a classé « NC » (+0,18 %) car sa « ouverture » stampée à **8h Paris (4308)** était APRÈS le rallye depuis la vraie ouverture (4215). **La fenêtre de mesure 8h-arbitraire des actifs continus cache des pertes** → fausse le win rate. **Fix mesure EN ATTENTE du GO fondateur** (change la sémantique du WR). (4) **Régression corrigée** : le gate ⚑ « événement majeur imminent » s'allumait globalement (FOMC-eve → cacao/café) → rendu ASSET-AWARE. 1294 tests (22 env-only). **Reco fondateur : régénérer la clé Twelve (a transité).** |
| 2026-06-15 | orchestration + @fullstack | **Angle mort overnight/week-end des actifs continus + dossier réforme « taux réels » (S7)** | Diagnostic sur pièces : l'or lundi 7h = close de vendredi (`gap_overnight≈0`) → les critères des **8 actifs continus** (or/argent/pétrole/cuivre/cacao/café/blé/eurusd) décidaient sur le close J-1, aveugles au mouvement overnight/week-end (légitime pour les indices cash fermés à 7h, FAUX pour les continus). **Volet A** : `fetch_twelve_series` enrichit la série avec le prix temps-réel quand plus frais (chokepoint unique, scope `actif_group=='continu'`, no-op sûr sur Twelve free). **Volet B** : drapeau ⚠️ « bouge à contre-sens depuis la dernière clôture vue » sur les continus de la Sélection. **Volet C** : cutover — 8 continus reset au 15/06 (3e reset assumé, vrai défaut). **Effet live à confirmer** (pas de clé Twelve en conteneur → logique testée par mocks). **Dossier ouvert (tête du ticket C)** : critère **taux réels US** (poids 12 sur l'or, signe FIGÉ, niveau-snapshot) a battu le système sur l'or **3×** par la même cause (2025, 11/06, 15/06) — le fix prix-frais est nécessaire mais NON suffisant : réformer ce critère (delta/régime, ou pertinence 24h coupée), preuve forward au WR or. 1269 tests. |
| 2026-06-12 | orchestration + @fullstack (×2) | **S7 après-midi — « Sélection du jour » mesurée + capteurs courts shadow** (décision fondateur : « 2-3 bons paris/jour, pas 12 ») | **Étage 1** : bloc `🎯 Sélection du jour — max 3` en tête de bulletin (conviction forte + couverture ≥0,70 + un pari par driver + avertissement catalyseur J0 ; zéro qualifiée → « ne pas forcer ») ; champ `selection_du_jour` au decision-log ; **WR Sélection mesuré à part au Bilan 22h15** (le sélecteur est lui-même en procès shadow). **Étage 2** : capteurs courts SHADOW poids 0 (`shadow_retour_j1`, `shadow_gap_overnight`) tracés au decision-log — zéro cutover, promotion sur preuve au ticket C. **Contre-lecture décisive** : le gap était None pour toujours (stamp émission posé APRÈS l'écriture du decision-log) → stamp anticipé (idempotent, étape 3 = no-op). **Dédup par FAMILLE macro** (`FAMILLES_MACRO` dans shared_drivers : taux_dollar/risk_on_off/metaux_croises/stress_europe/tendance_propre:PAR-ACTIF, fallback singleton) — rejeu 11/06 : Argent retenu, EUR/USD écarté « même pari (taux/dollar) », la sélection serait passée de 0/3 à 0/2+Café. Confiance déclarée : filtre 7/10, résultat 3/10 tant que l'edge n'est pas prouvé — c'est le WR Sélection qui tranche. 1221 tests. |
| 2026-06-11 | orchestration + @fullstack (×4) | **S7 matinée — validation run 7h + lisibilité décision** | **Run 7h analysé** : météo cacao RÉPARÉE (0,416 — durcissement suffisant), 3 critères ressuscités vivants (usd_brl 5,19 / gap_rv_iv −1,31 / balance_ez), momentum v3 en action (**cacao LONG→SHORT** — bug d'origine enterré, or mom. −1,0), Lot E confirmé (flux dédiés 43-97 gardés), couvertures ↑ (cacao 50→65, café 60→73, VIX 77→87). **Sonde 8h** : schedule GitHub a sauté (0 run) → **relais fiable ajouté** : le slot stamp 8h (VPS 8h15) dispatche la sonde (cycle.yml, actions:write, garde fenêtre 8h00-8h30, TEMPORAIRE). **GNews 400** : split insuffisant → requête quoted (verdict au prochain cycle). **Bulletin** : conviction binaire → libellés explicites (forte/contestée/fragile/zigzag/molle, logique inchangée) + **phrase d'explication déterministe sous chaque top** + **colonne « Porté par » + ⚭ même-pari** dans À jouer (réponse à « 3 forte = 1 seul pari taux/dollar »). **Page** : UX étendue aux 4 vues (intro WR/warm-up/08-08, lignes N=0 grisées, titre semaine humain, historique groupé par jour + créneau, bandeau toutes vues). **Fix échec réel masqué** : test_cutover_v2 (VIX au registre — oubli autopilote) ; leçon : vérifier soi-même la liste FAILED, pas le résumé des agents. Décision produit : émission reste à 7h (look-ahead : le call doit précéder la réf 8h05). 1185 tests. |
| 2026-06-11 | orchestration + @fullstack (×2) | **S7 matin — analyse run 7h + finitions** | **Run 7h24 validé sur pièces** : météo cacao RÉPARÉE (0,416, durcissement suffisant), momentum v3 en action (**cacao LONG→SHORT** — bug d'origine enterré), 3 critères ressuscités vivants, Lot E confirmé (flux dédiés 41-97 gardés), couvertures ↑ (cacao 50→65, café 60→73, VIX 77→87). **Sonde 8h : schedule GitHub a sauté (0 run)** → relais TEMPORAIRE dans cycle.yml (slot stamp 8h, déclenché VPS 8h15, dispatch API non-bloquant, garde fenêtre ≤8h30, `actions:write`). Requête gnews risk-off **toujours 400 malgré le split** → syntaxe quoted (`"risk-off"`, verdict au prochain source-health). Décision : **R1 reste à 7h** (émettre à 8h15 = APRÈS le stamp 8h05 = look-ahead structurel). **Conviction lisible** : libellés explicites à logique identique (forte / contestée ↯ / fragile ◧ / zigzag ⇆ / molle ≈) + **phrase d'explication déterministe** sous chaque top (2 critères porteurs + ↯/📰). **UX des 4 vues** (Résultats : intro warm-up+jalon 08/08, grisage N=0, explication flip/continuation ; Bilan semaine : titre humain ; Historique : dates lisibles+créneau+jour courant ; bandeau MODE TEST partout). **Fix échec réel masqué** : test_cutover_v2 (VIX au registre — oubli autopilote) ; leçon = vérifier soi-même la liste des FAILED, pas le résumé des agents. 22 failed env-only vérifiés un à un. |
| 2026-06-10 | orchestration + @fullstack (×3 vagues) | **Lot autopilote « sources & critères »** (analyse sources → « implémente absolument tout ») | **Santé critères persistée** (`criteres-health.md`, motif+cause HTTP par n/a — run 11/06 = verdict météo cacao CI-only) + durcissement Open-Meteo. **3 critères ressuscités** : café `usd_brl` (6), VIX `gap_rv_iv` (5, RV SPY−IV CBOE calculé), EUR/USD `balance_commerciale_ez` (3, Eurostat sans clé, validé live). **VIX rejoint ref-changed 11/06 → 12/12 actifs en ère v2** (addendums KILL/SELECTION). **Calendrier éco statique** (FOMC 2026 datés officiels, reste en règles « ~ », zéro date inventée) → Catalyseurs J+1 + **gate ⚑ déterministe FOMC J-1/J0** (flag hors score). **Fallback Stooq dormant** (séries critères only, JAMAIS stamps, tout-Twelve OU tout-Stooq, 404 conteneur → re-valider CI). **Écartées zéro-invention** : WASDE/CropProgress (→ secret `USDA_API_KEY` gratuit, impact blé p.11+6), WGC, ICE, AAII (→ option `v3/data/manual/`). 1153 tests. |
| 2026-06-10 | orchestration + @fullstack (×2, parallèle) | **Implémentation intégrale des 2 audits page (UX + orchestrateur)** — présentation pure | **Bulletin** (`scoring_analyste.py`, rendu md only) : section `## 🎯 À jouer aujourd'hui (24h)` (12 cellules 24h triées, scindées Jouables/À éviter ⚪≈🚫, conviction = seuil bilan_jour réutilisé, prix de réf. d'émission, drapeaux visibles) ; top multi-horizons AVEC drapeaux + alerte ⚭ driver commun ; `## 🔎 Calls 24h jugés` (ex-« Audit de la veille ») : tri date desc + synthèse X✅/Y❌ + étiquette `[mesure v1]` via `prix_reference_source` ; flips de bruit marqués ⚪/≈ ; hash/version en pied. **Page** (`build_html.py`) : bandeau contexte permanent (système + MODE TEST 08/08 + win rate only), fix ⚑ header, grisage cellules faibles, badge 🔴/🟡 dérivé des drapeaux existants, repli Limites/Calls jugés/Détail (ouverture au clic subnav, compat ancien titre), métadonnées en `<details>` pied, dates lisibles, message bilan semaine, **séquence verdicts ✅❌⚪ par cellule** (vue Résultats, rendu pur measures-log). Zéro seuil inventé, zéro impact scoring/mesure. 1085 passed (22 env-only). Effectif au bulletin du 11/06 7h. |
| 2026-06-10 | @ux | **Audit UX page HTML — 3 casquettes** (`v3/audit/audit-page-html-2026-06-10.md`) | Comprendre 5/10 · Décider 6/10 · Juger 5/10. **3 P0** : ⚑ header ≠ bulletin (vocabulaire), aucun contexte système visible, Briefing 7h découplé de la vue Aujourd'hui. **5 P1** : matrice non filtrée (quasi-neutres non grisés), flips non mis en avant, score illisible sans calcul, métadonnées debug visibles, Limites du jour non repliées. **Top 5 recos toutes sans toucher au scoring** : replier Limites+Audit veille / corriger ⚑+bandeau contexte / griser cellules faibles / unifier vue journée / badge conviction. Zéro invention de métrique, WIN RATE ONLY respecté. |
| 2026-06-10 | orchestration + @fullstack (×2 vagues) | **MOMENTUM v3 — fork tranché : design amendé COMPLET** (gel moteur levé sur dispatcher + cap UNIQUEMENT) | 2 constats experts re-vérifiés sur pièces avant d'agir. **Vague 1 moteur** : A1 `_twelve_variation_zscore` (VRAIE variation 20j → z-score de la série de rendements, plus le niveau laggard), A2 cap anti-inversion **aveugle au momentum** (`cap=\|quant−contrib_momentum\|×0,8`, pm1+pondéré, champs decision-log `contrib_momentum`/`cap_quant_ex_momentum`), A6 momentum exclu de la couverture (num+dénom, filet 📰 préservé), A7 test look-ahead close J-1. **Vague 2 fiches/mesure** : A3 poids **6 partout** (cacao 9→6, café/blé/cuivre 8→6, petrole/or/argent 7→6, prove-first) ; **famille finie** : eurusd (poids 5, `EUR=X`→`EUR/USD` via market_data), sp500/nasdaq/cac40 (poids 4, proxys SPY/QQQ/^FCHI, RSI 2 conservé), **VIX exclu** (A9) ; A5 métrique **« FAUSSES aux retournements »** (cap déclenché OU news⊥quant-hors-momentum ; affichée au Bilan du jour ; forward-test J+60=08/08) ; **cutover : 11 actifs reset au 2026-06-11** (8 existants avancés 10→11/06 + eurusd/sp500/cac40 ajoutés ; seul vix garde v1) + addendums KILL/SELECTION + CHANGELOG. Contre-lecture : commentaires YAML périmés (« poids=9 », « z-score-closes ») réalignés. **Sonde 8h débloquée** : cron temporaire 08h05 Paris jours ouvrés + garde fenêtre heure-Paris (NO-OP vert hors 8h00-8h30), à retirer après verdict A/B. Vérifs sur pièces Thomas : 3/3 PASS (poids=max, ref-changed 8 actifs, M7 borné+test). 1054+16 tests verts (22 failed env-only conteneur). |
| 2026-06-10 | orchestration + @data-analyst + @fullstack | **Fiches v3 — critère tendance-prix (correctif famille)** | Balayage (`momentum-prix-sweep.md`) : **8/12 fiches sans AUCUN critère de tendance-prix** = incohérence avec la thèse trend-following (révélé par cacao Lot F, validé Thomas « correctif de famille »). Ajout `momentum_prix_20j_<actif>` (zscore niveau 60j, **signe +1**) sur **7 fiches commodities/métaux** (cacao 9 / café/blé/cuivre 8 / petrole/or/argent 7) + cacao `hf_positioning` 7→5. **Gel moteur** (mapping `TWELVE_SYMBOLS` only, chemin z-score-closes existant). **Cutover étendu → 8 actifs reset** (cacao/petrole/nasdaq/argent/café/blé/cuivre/or ; 4 gardent v1 : sp500/eurusd/cac40/vix). ⚠️ **Décision ouverte** : momentum = z-score NIVEAU (gel-safe, consistant `sox_trend`) vs z-score VARIATION 20j (vrai rendement, +1 ligne moteur). EUR/USD (ABSENT) + indices = tour suivant. |
| 2026-06-10 | orchestration + @fullstack + @data-analyst | **Audit Cowork 10/06 — fiches v2 (Lots A/C/D/E/F) + cutover v2** | **Contre-audit décisif** : sur 9 doublons annoncés, **8 inexistants** (critères renommés 7-8/06 — `fiches-v2-dedup.md`) → checkpoint a évité de casser 9 fiches saines. **Lot A** : 4 fusions réelles (cacao/petrole/nasdaq/argent, poids=max). **Lot C** : compteur `n_regimes` (indicateur). **Lot D** : `is_news_regime` conservé (rare, modifie le score) + `p2_M7_ratio_news` borné [0,1] (décisionnel inchangé). **Cutover v2** : `system_version` + `ref-changed.json` (clé `ticker_principal`), reset **PARTIEL 4 actifs** (8 autres gardent v1), addendums datés KILL/SELECTION, **L023 propagé** (agréger par `cle`). **Lot E** : famine FIFO (`MAX=80`) réparée → round-robin + MAX 240 + fraîcheur ingestion (flux dédiés écrivaient 0 ligne ; comptage brief = artefact `cours`). **Lot F** : dédup cacao **insuffisant** → reco fiche v3 (momentum prix manquant, NON implémentée). **`+FRED` clos = NO-GO** (OOS 46,1%, FRED réellement appelé). 1044 tests. Gel scoring/WIN RATE ONLY. |
| 2026-06-10 | orchestration + @fullstack | **Fix backtest cache (faux vert) + relance `+FRED`** | Run `backtest-v2-fred` #1 = **success mais CREUX** (0 date testée partout, aucun `fred__`). Cause : nom de fichier cache embarque `end=today+1` → cache miss 100% un autre jour → fallback yfinance bloqué CI → vide. Fix : **cache-hit via glob** `{prefix}__*.csv` + slice (zéro invention) + **garde-fou `BacktestNoDataError` → exit 1** (fin du faux vert). +8 tests sans yfinance, 29 verts. Workflow relançable tout jour. Backtest reste **écarté comme jalon** (juste clore l'inconnue `+FRED`). |
| 2026-06-10 | orchestration + @fullstack + @data-analyst + @infrastructure | **Audit mesure externe 10/06 — 3 lots (Cowork)** | **Lot 2** : métrique **WR tradable** = VRAI/(VRAI+FAUSSE+NC) à côté du WR conclusif (NC = jour en position → capte le WR exécutable), kill criterion inchangé, +12 tests. **Lot 3** : `v3/SELECTION-RULE.md` **gravée signée Thomas** — trade 24h-only si **WR tradable ≥70% / N≥15** à **J+60=2026-08-08** (J0=09/06), sinon revue J+90 ; objectif = trade sur 1 jour, J+60 = point de contrôle (pas durée de détention). **Lot 1** (réf 8h US) : **prove-first** — sonde + workflow CI `probe-futures-8h` (read-only), **aucune fiche touchée** ; verdict A/B non concluable sans run CI 8h-8h30 Paris (hypothèse probable B = ES/NQ/VX absents Twelve free → S&P/Nasdaq/VIX restent 15h30 shadow-only, univers tradable = 8 continus + CAC). Gel scoring/WIN RATE ONLY respectés. Tout mergé sur `main`. |
| 2026-06-08 | @fullstack | **Backtest quant v2** (`v3/backtest/` : FRED+COT as-of, 7j/1m, 12 actifs, ablation) | **Verdict NO-GO** : pooled OOS **47,4 %** (sous le hasard), edge **décroissant** avec l'horizon (contraire au trend-following). FRED câblé (fix bug TIP inversé) mais **non testé** (pas de `FRED_API_KEY` dans l'env → arm `+FRED`=`price-only`) ; COT actif → +COT dégrade légèrement. Aberrations signalées : biais LONG OOS 82-91 %, Or à contresens (signe à revoir), N minuscule en 1m. *Alternative : Thomas a ensuite **écarté le backtest comme jalon** (05/06) — validation = shadow réel uniquement.* |
| 2026-06-05/08 | 3 experts (Analyst/NewsTrader/Spéculateur) | **Audits trio session 4** (`v3/audit/*-2026-06-0[58].md`) | (1) **Corrélation cachée #10** : ~40 % des SHORT portés par le seul TIPS. (2) **Enquête Or #8** : régime, PAS bug de signe (l'or a monté malgré taux réels hauts en 2025) → reco ticket C. (3) **Cohérence des 36 décisions** : consensus « ne pas suivre » CAC 24h (coin-flip), Cuivre 7j/1m (COT vs news), S&P 24h, Blé (mono-critère) ; noyau solide Argent/Nasdaq/Or/EUR-USD. (4) **Audit Journaliste = LÉGER** (Thomas avait raison) : pas de 1m mesuré, N_brut ×3 trompeur, 34 % non-conclusives. (5) **Audit spec 5 rapports** : 7,5/10 → 15 corrections → spec v2 9,5/10 → **revue finale 8,7/10 GO MERGE**. |
| 2026-06-08 | @fullstack + @infrastructure | **Corrections audit + micro-bugs + retrait mining_com** | **Drapeau ◧ mono-critère rendu visible** (A1) + champ `quasi_neutre` (A2). **2 micro-bugs** : nom de fichier bulletin en heure Paris (était UTC) ; **mining_com 403 Cloudflare persistant CI → RETIRÉ** (le retry n'a pas suffi, flux non-unique). **B1 CAC** (contre-momentum, signe OAT-Bund correct) + S&P/Argent/échelles → **reco Lot 4b / ticket C** (pas appliqué, mesurer avant d'agir). |
| 2026-06-08 | @fullstack | **Refonte « 5 rapports » mergée + VPS déployé + page complète** | Chantier autopilote **bout-en-bout mergé sur `main`** (mesure ouverture→clôture, 5 rapports, Manager). **VPS Anya déployé** (8 créneaux, OK 204, premier run live vert). **Page (`build_html`) réparée** (2 passes) : affichait QUE le briefing 7h → ajoute suivis 12h/18h + bilan jour + bilan semaine + **tableau win-rate** (`performance.md`, jamais branché avant). 984 tests, shadow préservé. |
| 2026-06-09 | @fullstack | `build_html.py` : tableau win rate affiché sur la page | Manque corrigé : `v3/data/performance.md` (notre tableau win-rate-only par actif × horizon) n'était référencé nulle part. Nouvelle vue sidebar « 📈 Résultats / Win rate » + win rate **en tête** de l'onglet Historique, ancien A/B Taux/Brier **rétrogradé** (`<details>` replié). `load_performance_md`, dégradation propre, WIN RATE ONLY. 8 tests (`test_build_html_winrate.py`), suite verte 984 passed. |
| 2026-06-09 | @fullstack | `build_html.py` : affichage des 5 rapports sur la page | Bug corrigé : la page n'affichait que les briefings 7h. Ajout collecte suivis 12h/18h + bilan du jour 22h + bilan semaine. 2 vues sidebar : « Aujourd'hui » (regroupé par jour) + « Bilan semaine ». Dégradation propre si dossiers vides. WIN RATE ONLY. 9 tests (`test_build_html_reports.py`), suite verte 977 passed. |
| 2026-06-01 | orchestration + @fullstack | Correctifs pipeline run quotidien | Routing IA-first réparé, prix d'émission, symboles Twelve validés. Synthèse directionnelle 2 niveaux + contexte-prix. **Indices via ETF Twelve** (yfinance bloqué CI). **Rate-limiter Twelve** : attend au lieu de rejeter, `TWELVE_RPM=55` (plan Grow). PROBA_SCALE 10→15, propagation reliability, garde chevauchement 7j/1m. |
| 2026-06-01 | 3 experts (Analyst / News Trader / Spéculateur) | `v3/audit/chaine-*.md` + `coherence-3-experts.md` | Audit des runs dans l'ordre d'édition. Cohérence : 2 faux positifs écartés sur preuve (signe géopol déjà câblé `ia_synthese` ; contamination 2025 filtrée par cutoff lookback). **Trio = panel d'audit officiel** (`v3/audit/README.md`). |
| 2026-06-01 | @fullstack + 3 experts (validé Thomas) | **Plan horizon** (`revue-plan-horizon-*.md`) | Constat : DeepSeek = 1 direction/actif **horizon-agnostique** ; l'horizon est géré par `pertinence` par critère. **PAS de decay_factor global** (rejeté 3/3 : doublon + casserait l'OPEC structurel). Retenu : recalibrer pertinence (or/petrole/vix, OPEC 7j-1m préservé) + cap anti-inversion α=0.8 (override si high+confirmed) + `ratio_news`/drapeau 📰. Preuve : Or 24h & VIX 1m → SHORT, Pétrole/S&P inchangés. |
| 2026-06-01 | infrastructure (orch) | Workflow `cycle-decision` | Diagnostic : `schedule` GitHub retardé 1-3h (pas une panne). Redondance ×3 + garde-fou anti-doublon. Read/write permissions activées. |
| 2026-06-01 | @data-analyst | `v3/audit/spec-phase2-news-analyst.md` | Spec Phase 2 News lot cœur : `event_id` (SHA-256/12 + dédup floue Levenshtein ≤15 %), `event_date` (fraîcheur RSS pubDate vs ingestion), `nature` (DeepSeek — 5 valeurs fermées, verbal/deja_cote/non_tradable exclus du scoring), gate fraîcheur FRESHNESS_OVERRIDE_DAYS=3j sur override high+confirmed, STALE_THRESHOLD=30j. Zéro double-amortissement avec pertinence. Shadow direct : 7 métriques M1-M7 dans decision-log (champs `p2_*`). PROMPT_VERSION bumper v2.1→v2.2. |
| 2026-06-01 | @data-analyst | `v3/audit/chaine-analyst.md` (écrasé — run 2053) | **Validation méthodo Phase 2.** T1/T2=0 = angle mort instrumentation (pas anomalie) — proposé `p2_shadow_contrib_exclu` + `p2_shadow_flip_potential` pour rendre T1 mesurable (A1-P0). Dédup CAC40/GOLD >30% = vrais quasi-doublons, garde-fou légitime. 399 reposts / 397 stale plausibles — EIA source de fond à exclure du compteur taux_stale (A4-P1). Zéro double-amortissement si coef_nature binaire (spec) — à vérifier si flottant en implémentation (A3-P0). T1/T2 significatifs à 30 runs (J+10). **Verdict : GO 7,5/10.** |
| 2026-06-01 | @data-analyst | `v3/audit/audit-reel-et-backtest-scope.md` | **Audit honnête données réelles + protocole backtest.** N_eff=0/12 cellules → aucune affirmation statistique possible. Wilson N=1 : borne basse 20.6% (seuil opérationnel 50%). Puissance stat à N=1 : ~5% = pile ou face. Biais LONG 61% : borderline (p~0.05 à N_eff_ind≈60) — régime ou structurel indiscernable sans 30 runs. Seuils à revoir : VIX 5%→3%, Cacao 1.5%→1.0%. Backtest quant-only protocol défini : 4 ans historique, non-chevauchant (24h quotidien/7j hebdo/1m mensuel), walk-forward IS:2021-24 / OOS:2025-26, critères GO : directional accuracy OOS≥60%, Wilson_low≥55%, p-value bootstrap<0.05. Effort : 5-7j dev. |
| 2026-06-02 | @data-analyst | `v3/audit/gates-analyst.md` | **Concertation round 1 — grille complète des gates méthodologiques.** 46 gates définis sur S1-S9 (S5 exclu). 12 P0 identifiés, dont Top 5 critiques : borne z-score (S4-G4), division par zéro écart-type (S4-G5), exclusion nature Phase 2 du scoring (S2-G5), zéro look-ahead Journaliste (S8-G2/G3), réconciliation somme contributions (S6-G1). 12 gates automatisables en CI avant sortie shadow. Gates revue humaine : révision seuils S8-G4, chevauchement S8-G7, enum nature Phase 2 — requis avant émission réelle. |
| 2026-06-02 | orchestration (édition directe) | Seuils mesure + heure bulletin | Resserre seuils 24h **VIX 5→3%**, **Cacao 1.5→1.0%** (audit réel : réduit les non-conclusives, AVANT accumulation pour éviter biais rétrospectif). Heure du run dans le titre du bulletin (visible HTML). *Pourquoi édition directe : config 1 ligne + cosmétique, hors périmètre agent (commandement 4 exceptions).* |
| 2026-06-02 | @fullstack | `v3/backtest/` (backtest quant v1) | **Backtest historique no-look-ahead** (yfinance, walk-forward IS 2022-24/OOS 2025-26, non-chevauchant). Verdict v1 **NO-GO** : POOLED OOS **50.8%** = pile ou face sur 4 actifs × 24h **price-only**. PARTIEL : COT/FRED absents (~50% du poids) → v2 obligatoire pour conclure. Bug trouvé : proxy ETF TIP inversé. *Alternative écartée : POC limité — Thomas a choisi le vrai backtest.* |
| 2026-06-02 | @fullstack | Bilan news + `source_monitor` | **Bilan des news** (calls portés news VRAI/FAUX dans le bulletin — juger le jugement DeepSeek). **source_monitor** : santé par flux/cycle (appelé/OK/échec/muet + reçus vs gardés + raison) → `source-health.md` + bloc bulletin. Audit 4 flux muets : gnews_cac40 (whitelist FR ajoutée), gnews_wheat (query élargie), investing_stocks (doublon→news_287), mining_com 403 (visible). |
| 2026-06-02 | 3 experts (NT/Analyst/Spé) ×3 rounds | `v3/audit/audit-requetes-news-*.md` | **Optimisation requêtes news jusqu'à 10/10** (itération demandée Thomas). Comble Nasdaq (Nvidia/IA/semi) + VIX (volatilité + causes amont). Sépare Fed/BCE, S&P earnings, retire DAX, dégroupe Or/Argent, +CB-gold/solaire/EUDR/WASDE/café-gel/blé-GASC/CAC-SBF120. 14 requêtes, 22 flux. **Plafond news ~9,5-10** (reste = pipeline data : CFTC/CBOE/GASC). |
| 2026-06-02 | @fullstack | **Gate S5 suffisance de données** | Couverture pondérée Σpoids(critères avec donnée)/Σpoids total. Paliers : ≥65% normale · 40-65%/périmé `⚠️ conf. faible` · <40% `🚫 insuffisant` (override jamais-neutre, exclu mesure VRAI/FAUX + biais). Sécurité demandée Thomas (plus de note confiante sur 2 critères/9). *Pourquoi code et pas DeepSeek : couverture = calcul déterministe ; DeepSeek réservé au jugement news.* |
| 2026-06-02 | 3 experts ×2 rounds + orchestration | `v3/audit/gates-FINAL.md` (concertation) | **Liste finale convergée des gates** (débat contradictoire R1+R2). 9 gates consensus + roadmap 6 lots. 3 frictions tranchées : C3 = détecte/drapeaute (cap α arbitre, pas trend-first dur) ; C8 scindé C8a P0/C8b P1 ; C4 = plafond mono-news + materiality×reliability (pas de quorum bloquant). |
| 2026-06-02 | @fullstack (×6 lots) | **6 lots de gates** (cf. État) | Lot 1 fondation déterministe (z-score borné/std0/spike→n/a + réconciliation Σ + UTC) · Lot 2 C1 signe DeepSeek (neutralise news au sens inversé) · Lot 3 C5 intégrité mesure (verrou prix/échéance/look-ahead) · Lot 4a détecteurs flag-only (↯⇄⇆) · Lot 5 sanity news (⌛⊘) · Lot 6 publication (« cellules à surveiller » + flip/continuation). 478→619 tests. **Lot 4b (actions) différé** : calibrer sur fréquence mesurée des détecteurs (choix Thomas « mesurer avant d'agir »). |
| 2026-06-02 | orchestration (édition directe) | **Pinger VPS — fiabilisation du cron** (`v3/ops/vps-trigger/`) | Le `schedule` GitHub abandonne des créneaux (matins 01-02/06 sautés, prouvé par l'absence de runs `schedule`). Cron local sur le VPS Anya → `workflow_dispatch` (ref=main) 3×/jour via `/etc/cron.d/tradingapp` (run root, survit aux redéploiements Anya). Compute/stockage restent sur GitHub Actions (archi v3 « sans VPS » préservée — le VPS n'est qu'une horloge). Validé prod : runs #23 (MCP) + #25 (VPS) HTTP 204 → success. Schedule natif gardé en fallback. **Leçons** : curl mono-ligne (continuations `\` perdues au paste SSH) ; un PAT collé en chat = compromis → révoqué + régénéré. |
| 2026-06-02 | orchestration + 3 experts + @fullstack | **Audit trio du bulletin + correctif FRED-429 + fix cron TZ** | **Trio** (Analyst/News Trader/Spéculateur) sur bulletin 12h29 : **AJUSTER 6/10** unanime (`v3/audit/bulletin-*.md` + `coherence-bulletin-2026-06-02.md`). Noyau sain : Or SHORT, Pétrole/Argent LONG. **Audit des recos avant d'appliquer** : le « 42% INSUFFISANT » = **FRED 429** (rate-limit → `DFII10`/TIPS morts), PAS un seuil ni un trou d'archi → **corrigé** (retry/backoff + throttle FRED, commit `a03cd98`, +6 tests). Coin-flips Nasdaq/VIX 24h : **gardés flag-only** (décision Thomas — Lot 4b différé maintenu). Cron : `CRON_TZ` inopérant sur Vixie/Ubuntu (midi 02/06 raté) → bascule **cron horaire `0 * * * *` + garde heure-Paris dans le script** (robuste DST). **FRED vérifié en prod** (run #28) : `fred_dead:DFII10` disparu, TIPS revient. **Généralisé** : helper partagé `http_retry.py` (429/5xx + `Retry-After` + throttle par bucket) → GNews/NewsAPI + UA Chrome pour le 403 mining_com (commit `e28f40f`, +16 tests, 632 verts). **Durcissement HTTP complet** (commit `6c1fe8e`) : `http_get_json` CFTC/EIA/Open-Meteo passé sous retry (wrapper préservant le contrat) + `fred_spread_thin` DGS10-DE résolu (forward-fill LOCF de la série DE mensuelle sur la grille US quotidienne). **Toute la chaîne HTTP du pipeline est désormais résiliente au 429/5xx** — prêt pour tests complets. |
| 2026-06-05 | @fullstack | **Corrections audit trio bulletins live 05/06** (A1/A2 flag-only) + recos | **A1** : mono-critère rendu VISIBLE (`◧` matrice + surveillance + légende) — la détection marchait déjà (CAC ET EUR/USD = True au decision-log, « incohérence » de l'audit inexistante), mais le flag était SHADOW-only. **A2** : champ shadow `quasi_neutre` (`\|score\|<0.30`) au decision-log — Cuivre 7j (-0.22) raté par coin_flip (0.05, **non touché** car couplé EPSILON_CARRY/contradiction carry). **Enquêtes reportées** : B1 CAC « à l'envers » = signe OAT-Bund **correct** + contre-momentum `⇄` → **Lot 4b reco** (pas réactivé) ; B2 Cuivre/C1 S&P/C2 Argent/C3 échelles = **ticket C reco** ; DXY 118.9 = faux positif (DTWEXBGS≠DXY, sain). Tout flag-only, 0 impact conclusion/mesure. **873 tests**, 0 régression. Doc clé `v3/audit/reco-corrections-2026-06-05.md`. |
| 2026-06-02 | @fullstack | **2 bugs de données (audit trio)** : critères composite/mapping jetés + source VIX incohérente | **BUG#1** : `normalise()` ne gérait pas `composite`/`mapping_non_monotone` → tombaient en « type inconnu » et étaient écartés du score malgré leur `valeur_normalisee` déjà émise par criteres_calculator. Critères à fort poids récupérés : café météo Brésil (poids 11), cacao HF positioning (7), S&P VIX régime (8), CAC V2X (8), Nasdaq VXN (7). Fix : consommer la normalisee pré-calculée (comme zscore), n/a propre sinon. Aucune correction d'émission nécessaire (handlers OK). **BUG#2** : `vix_regime` lisait ^VIX via Twelve (23.6 périmé) vs `niveau_vix_absolu` via CBOE CSV (14.95 frais) — root cause = sources distinctes même sous-jacent. Fix : `_handle_mapping_non_monotone` lit CBOE en priorité pour régimes VIX (`MAPPING_REGIME_CBOE_INDEX`), fallback Twelve pour VXN/V2X. VIX 14.95 → +0.975 (régime sain) au lieu de -0.323 (faux stress). +12 tests, 704 passed / 8 failed pré-existants (env-only) inchangés. Commit `7bbe128`. |
| 2026-06-05 | @infrastructure | `.github/workflows/backtest-v2.yml` + cache prix/COT committé + doc REPORT | **Outil pour relancer le backtest v2 AVEC FRED** (point #11 Niveau 3 — clore l'arm `+FRED`). Workflow `workflow_dispatch` **MANUEL only** (jamais de cron → n'interfère pas avec `cycle-decision`, shadow préservé), `permissions: contents: read`, **aucun commit auto** (l'exécution salit `v3/data/` via import `scripts/` → publie un **artifact** log/REPORT, pas un commit). **Stratégie cache** : yfinance bloqué sur runners CI → cache prix/COT (`v3/backtest/.cache/*.csv`, 35 fichiers/3,6 Mo, OHLCV+COT publics, 0 secret) **committé** (`git add -f`) ; CI lit le cache (0 yfinance) + appelle **FRED réel** (clé secrète, `fred__*.csv` **NON committés** exprès) + Socrata CFTC. Fallback VPS Anya documenté. Tests `test_backtest.py` **21 verts**. **Non déclenché** (Thomas lance). |
| 2026-06-06 | @infrastructure | **Garde de run étendue aux fériés de marché** (`cycle.yml` + `journaliste.py` + tests) | **Demande fondateur (« point final ») : rapports UNIQUEMENT les jours de bourse OUVERTS.** Le week-end était coupé mais pas les **fériés** (un lundi férié NYSE/Euronext produisait un bulletin sur prix figés). **Fix sans duplication** : nouvelle fonction importable `is_trading_day(d)` dans `journaliste.py` = `jour ouvré and not _is_market_holiday(d)` → **réutilise le calendrier `MARKET_HOLIDAYS` existant (1 seule source, zéro date recopiée)**. La garde de `cycle.yml` appelle ce Python (`python3 -c`, stdlib only, avant setup deps) au lieu du test week-end bash : bourse fermée (week-end OU férié) + pas de force → NO-OP avec log clair. `force`/`RUN-CYCLE.txt` bypass préservé, anti-doublon ×3 schedule-only préservé. VPS inchangé (fast-path week-end ; fériés autoritaires côté workflow — le VPS ne recopie pas de liste). **Bug heredoc indenté évité** (passage à `-c`). Tests : `test_is_trading_day.py` 7 verts + `test-guard-logic.sh` 13 verts (param `holiday`). `v3/data/` non pollué. Aucun run déclenché, shadow préservé. |
| 2026-06-06 | @infrastructure | **Garde week-end étendue au VPS** (`cycle.yml` + `trigger-cycle.sh` + test) | **Bug** : briefing produit samedi 06/06 07h. **Cause** : garde week-end de `cycle.yml` limitée à `schedule`, mais le driver réel est le **VPS en `workflow_dispatch`** (traité comme forçage manuel → NO-OP week-end jamais atteint). **Fix** : input `force` (défaut false) + garde restructurée — forçage explicite (`force=true` OU push RUN-CYCLE) bypass tout ; sinon (schedule OU dispatch-VPS) → NO-OP week-end Europe/Paris ; jour ouvré → anti-doublon ×3 schedule-only (VPS 1 tir/créneau préservé). Pinger VPS reçoit aussi sa garde jour-ouvré (`date +%u ≥ 6 → exit`, défense en profondeur). **Aucun run de semaine affecté, forçage manuel préservé.** Test `test-guard-logic.sh` : 10 cas verts (6 clés). **Aucun run déclenché**, shadow préservé. |
| 2026-06-05 | @data-analyst | `v3/audit/correlation-cachee-2026-06-05.md` | **Analyse fausse impression de consensus (point #10).** Cartographie des 3 critères macro partagés : TIPS (4 actifs : Nasdaq/Or/Argent/S&P), DXY (6 actifs : Or/EUR/USD/Pétrole/Cuivre/S&P/Blé), HY spread (1 actif seul : S&P — pas un problème de doublons). Quantification : 10-11 cellules SHORT sur 25 (40-44%) portées à 60-90% par le seul signal TIPS. CED (Consensus Effectif Décorrélé) estimé à ~58-60% vs 69% brut. **Reco A (flag-only)** : bloc « ⚭ Drivers macro partagés » dans le bulletin — implémentable immédiatement par @fullstack. **Reco B (Ticket C)** : afficher CED en parallèle du comptage brut — formule PI à valider Thomas avant implémentation. Zéro modification de scoring/conclusions. |
| 2026-06-06 | @copywriter | `v3/audit/reco-wording-noms-criteres.md` | **Traduction en langage trader de 100 noms de critères** (12 actifs). Acronymes de sources (NOAA, CFTC, USDA, TIPS, COMEX, LME…) retirés ou traduits. 5 acronymes connus traders gardés entre parenthèses (PMI, RSI, VIX/VXN/V2X, SKEW/VVIX). Mini-glossaire 5 termes recommandé (verdict : OUI). Aucune logique modifiée. |
| 2026-06-06 | @fullstack | **Tableau « Détail par actif » reformulé en langage trader** (`scoring_analyste.py` + `build_html.py` + 3 tests) | Implémentation de la spec `reco-wording-detail-bulletin.md`. **En-têtes clairs** (9 col., « Note » supprimée) : Comment c'est lu / Valeur actuelle / Penchant / Importance / Sens / Effet 24h-7j-1m. **Types traduits** via `TYPE_NORM_LABELS` (zscore→Écart à la normale, lineaire→Échelle graduée, …, fallback sur le brut si type inconnu). **Sens humain** : normal (+1) / inversé (-1), — pour les gates. **Gate actif préservé** : « Drapeau régime ⚑ actif » dans « Comment c'est lu » (info de risque ex-portée par Note). **Encart « Comment lire ce tableau »** statique inséré 1× avant le 1er actif. Paramètres techniques (`centre=X echelle=Y`) retirés de la vue (restent au decision-log). Masquage mobile recalé (3e « Valeur actuelle » + 4e « Penchant » au lieu de la 10e « Note » disparue). **Zéro changement de chiffre/score/conclusion** — refonte de libellés uniquement. **893 tests verts**, `v3/data/` non pollué. Shadow préservé. |
| 2026-06-08 | @infrastructure | **Câblage cron/workflows des 5 rapports + stamps d'ouverture** (`cycle.yml`, `weekly-summary.yml`, `run_stamp.py`, VPS, table de vérité) | **Routage par heure de Paris (DST-safe, jamais d'offset en dur)** dans `cycle.yml` (step `Route`) : 07h→bulletin complet, 08h05/09h05/15h35→`run_stamp.py` (stamps d'ouverture prix-only, idempotents), 12h/18h→`run_suivi.py` (léger, PAS le bulletin — Q9), **22h15 Paris→`run_bilan.py`** (R4). Cron historique 7h/12h/18h inchangé + cron horaire `6,7,8,13,14,20,21` pour stamps/bilan (garde heure-Paris). Garde `is_trading_day` (jours bourse+fériés) + `force` préservée. **`run_stamp.py`** = runner léger (appelle `stamp_prix_ouverture(now)`, aucun scoring/DeepSeek). **`weekly-summary.yml`** (workflow séparé, Q7) : dimanche 18h, **bypass `is_trading_day`** + guard `weekday()==6`, force bypass (CA-W1c), garde-fou commit CA-W4. **VPS** : créneaux 07/08/09/12/15/18/22 + dimanche→weekly, crontab minute `0`→`15` (tir 22h15), flag `--check` (CA-I3). **CA-I1/I2/I3/I4 couverts.** Restes @fullstack (mesure, hors infra) : CA-M7, CA-B2. Gates : pytest 957 verts, `test-guard-logic.sh` ALL PASS, 2 YAML valides. Aucun run déclenché, shadow préservé. |
| 2026-06-08 | @fullstack | **`performance.md` refondu en tableau WIN RATE propre + archive hebdo figée** | Présentation only (logique de mesure inchangée). `render_performance` → tableau win-rate-only (`Actif \| Win rate \| Paris (réels) \| Non notés \| Statut`), groupé par horizon (24h→7j→1m), trié win rate ↓, **36 cellules dont 1m visibles**, statuts sans argent (`⏳ trop peu N/15` / `✅ objectif atteint` / `❌ sous l'objectif` / `⏳ en attente`), ligne synthèse `X/36 fiables`. **Retirés de la vue** : Brier, `Taux_brut`, « Alertes » (pavé), LONG/SHORT, P&L (conservés dans `measures-log.jsonl`/decision-log). **Archive hebdo figée** : `render_weekly_winrate` → `v3/data/performance/weekly/win-rate-{AAAA-Sxx}.md` (semaine ISO Europe/Paris, win rate cumulé + colonne « Nouveaux paris (semaine) ») — réécrit pendant la semaine, figé après. `build_html` inchangé (page lit `measures-log.jsonl`/`performance-ab.md`, pas la matrice). `test_winrate_view_weekly.py` (18 cas) + tests ancien format mis à jour. **913 tests verts**, `v3/data/` non pollué, shadow préservé. |
| 2026-06-08 | @data-analyst | `v3/audit/audit-spec5-analyst.md` | **Audit méthodo spec « 5 rapports + mesure ouverture→clôture »**. Note **7,5/10**. Cœur sain : modèle open→close fondé, 1 décision/jour corrige le gonflement ×3, WIN RATE ONLY tenu. 10 défauts identifiés dont 3 P0 : (D1) Manager déclenche propositions sur N=5 → sur-réaction petit N (relever à N_eff≥10+Wilson_low<50%) ; (D2) mesures 12h/18h existantes risquent de polluer les KPIs post-refonte (filtre bulletin_id 7h manquant dans CA-M6) ; (D3) CA-W1 ambigu sur force=true+weekly_summary hors dimanche. P1 : dates de sortie warm-up par horizon absentes (24h ~J+25, 7j ~15 semaines, 1m hors portée 6 mois), biais féries partiels non documenté, seuil 0.1% neutre sans constante nommée, test CI R2/R3 vs measures-log absent. |
| 2026-06-08 | @data-analyst | `v3/audit/audit-journaliste-analyst-2026-06-08.md` | **Audit Journaliste (mesure).** Verdict : **LÉGER — critique Thomas fondée.** Top 5 manques : (1) horizon 1m structurellement absent (0 mesure, décision impossible avant J+90) ; (2) N_eff max=6/15 — warm-up sur toutes les cellules, kill criterion non activable ; (3) N_brut trompeur ×3 par créneaux intrajournaliers (21 affiché vs 6 indépendants) ; (4) ratio non-conclusives global invisible ; (5) Brier non interprétable à N<10. Points solides : entry-lock immuable, look-ahead armé, select_non_overlapping correct, Wilson IC conservateur, exclusion INSUFFISANT/NON_NOTE, warm-up transparent. |
| 2026-06-08 | @fullstack | **Refonte 5 rapports finalisée : CA-M7 + CA-B2** (`journaliste.py`, `bilan_jour.py`, `test_bilan_jour_cam7_cab2.py`) | **CA-M7** : compteur `jours_bourse_exclus` (férié partiel — un marché ouvert, l'autre fermé → garde globale `is_trading_day` saute le run) visible dans `performance.md`. `is_partial_holiday(d)` + `compter_jours_bourse_exclus()` (purs) distinguent XNYS/XECB via lib `holidays` (Memorial Day=US fermé, 1er Mai=EU fermé) ; fallback sans lib → False (zéro invention, sous-estime). **CA-B2** : clôture CAC du 24h = close officiel 17h30 (bougie `1day` via `fetch_twelve_series`), groupe EU résolu par `mesure_ouverture.actif_group` (zéro heure en dur). **Fallback robuste Q5** (non validé shadow) : si pas de bougie datée J → dernier prix + marqueur `[close approx]` + note « à valider en live (Q5) », jamais de close fabriqué. WIN RATE ONLY, zéro modif silencieuse poids/seuils. Gate `pytest v3/ -q` **968 passed/3 skipped**, `v3/data/` non pollué, aucun run déclenché. |
| 2026-06-08 | @fullstack | **Phase 3 refonte 5 rapports : bilan de la semaine dimanche 18h + le Manager** (`run_weekly.py`, tests) | **Dernière phase — le Manager (4e agent historique) enfin implémenté.** `build_bilan_semaine(now=...)` → `v3/data/bilan-semaine/{ISO}.md` : win rate semaine (archive hebdo reprise telle quelle, CA-W2) + **win rate par conviction** forte/faible (réutilise `bilan_jour`) + **cellules porteuses** (ce qui MARCHE, §4.6) + cellules à surveiller + **propositions d'ajustement** (format strict §4.5) + observations + dates de sortie de warm-up. **Manager (P0 trio) : propose UNIQUEMENT si N_eff ≥ 10 ET Wilson_low < 50% ET ≥ 2 semaines CONSÉCUTIVES** (persistance snapshots `bilan-semaine/.state/{ISO}.json`, data/ jamais config/) ; N_eff 5-9 → observation sans proposition (mesurer avant d'agir). Propositions justifiées par chiffres réels du KPI (zéro invention). **Le Manager n'applique RIEN — `git diff v3/config/` vide après run (CA-W4, testé).** Réutilise `journaliste.measure()` (zéro recalcul), `ZoneInfo` (jamais d'offset en dur). **WIN RATE ONLY.** `test_run_weekly.py` 14 cas (CA-W1..W6 + garde-fous). **957 tests verts** (+14), `v3/data/` non pollué (`.gitkeep`), shadow préservé. **Restants infra (@infrastructure)** : CA-I2 (schedule dimanche 16h UTC + bypass `is_trading_day` + guard `weekday()==6` dans `cycle.yml`), CA-I3 (créneau dimanche VPS Anya). |
| 2026-06-08 | @fullstack | **Phase 2 refonte 5 rapports : suivis 12h/18h légers + runner bilan 22h** (`run_suivi.py`, `run_bilan.py`, tests) | **Rapports de SUIVI courts** (Thomas lit en 2 min) sur les positions 24h du Briefing 7h. **`run_suivi.py`** (`build_suivi(report_type, now=...)` → `v3/data/suivi/{date}-{12h\|18h}.md`) : statut vs SON ouverture (✅ gagne/⚠️ perd/— neutre), **dynamique de tendance** (↑ s'accélère/↓ s'essouffle/⇄ se retourne vs suivi précédent via snapshot 12h ; flags US ↗/↘ au 18h), **suggestion de sortie** (`Sortie à envisager` si `\|Δ%\| ≥ SEUIL_PCT_actif` CONTRE le call — **drapeau jamais un ordre**), **US à 12h = 🕐 pas encore ouvert** (ouverture 15h30, pas de ligne trompeuse ; 1er statut US au 18h), news courtes best-effort (zéro DeepSeek, Q9). **`run_suivi` n'écrit JAMAIS measures-log** (CA-S6/S6b — pas de cellule mesurée, pas de scoring). **`run_bilan.py`** : runner CLI de `build_bilan_jour` (R4). **WIN RATE ONLY** (le % de move OK, jamais d'€). CA-S1..S6b couverts ; restants = infra (CA-I*, Q9). **943 tests verts** (+15), `v3/data/` non pollué (dossier `suivi/`+`.gitkeep`), shadow préservé. |
| 2026-06-08 | @fullstack | **Phase 1 refonte 5 rapports : mesure ouverture→clôture + bilan 22h** (`mesure_ouverture.py`, `bilan_jour.py`, `journaliste.py`, configs, tests) | **Le 24h passe de « prix 7h » (prix de nuit, marché fermé) à « ouverture propre → clôture, jugé 22h ».** Démo Or 24h : ancien réf=3500→FAUSSE (artefact) vs nouveau réf=3400→VRAI (vraie tendance). **`stamp_prix_ouverture`** → `prix-ouverture/{date}.json` (après ouverture marché + délai 5 min, heures Paris via ZoneInfo, idempotent+entry-lock, Twelve KO→absent=suivi-interrompu, mapping groupe EU/US/Continu par famille+override nom, convention 08h continus assumée Q10). **Réf 24h/7j/1m = ouverture** (fallback émission+WARNING, Q3/CA-M5, `prix_reference_source` tracé). **Filtre 7h (CA-M6b)** : seul le Briefing 7h noté, suivis 12h/18h `non-noté` → **fin du gonflement ×3**. **`build_bilan_jour(now=...)`** (R4) : win rate du jour + **par conviction** forte/faible (CA-W6) + FAUX gros move (tri amplitude %, **PAS d'argent**) + news Option C + catalyseurs J+1 best-effort. **WIN RATE ONLY, 0 modif silencieuse, 928 tests verts** (+15). **Restants infra** : cron 22h15, CA-M7, CA-B2, CA-I1/I4. |
| 2026-06-08 | @product-manager | `v3/docs/reco/spec-refonte-5-rapports.md` | **Cahier des charges — 5 rapports/jour + mesure ouverture→clôture.** Design validé Thomas traduit en spec complète : (1) schéma 5 rapports (R1 Briefing 7h seul noté, R2/R3 suivis courts, R4 Bilan 22h note les 24h, R5 Bilan dimanche Manager) ; (2) modèle de mesure : prix-ouverture = référence 24h (nouveau fichier `prix-ouverture/{date}.json`), table heures par groupe EU/US/Continus ; (3) format exact R2/R3/R4/R5 ; (4) Manager : métriques, détection cellules/critères faibles, propositions format strict (Thomas valide, jamais appliqué seul) ; (5) infra : créneau 22h jours de bourse + dimanche 18h exception guard ; (6) 1 décision/jour = fin du gonflement N_brut ×3 ; (7) 31 critères d'acceptation testables ; (8) ordre de construction 3 phases. **9 questions ouvertes signalées** (Q1-Q9) — décision Thomas requise avant implémentation des blocs concernés. |
| 2026-06-25 | @creative-strategy (S10) | **Identité de marque produit** (`v3/docs/brand/identite-produit-s10.md`) | Rejet du nom « Issa Capital » (domaine != marque, erreur des agents précédents). Positionnement en 1 phrase (signal directionnel, 12 marchés, 3 horizons). 5 noms candidats avec reco N°1 : **Capvent** (cap + vent, ancré dans la métaphore validée par Thomas, distinctif, disponibilité .com probable). Tagline reco : « Le cap. Trois fois par jour. » 2 alternatives. Palette HEX complète (fond #FFFFFF pour zone marketing, bleu accent #2563EB, vert/rouge réservés aux signaux). Recommandation thème CLAIR pour hero/header/footer (contre le dark actuel). Typographie Inter 400/700/800. 5 anti-patterns identifiés (nom, visuel, discours). **DÉCISION THOMAS (25/06) : renommage REFUSÉ, on garde « TradingApp » (« quand est-ce que j'ai demandé Issa Capital / Capvent ? »). SEULS retenus : la direction visuelle CLAIRE façon Twelve Data (validée) + le retrait du branding « Issa Capital » de la page.** Leçon : ne pas confondre « identité visuelle » et « renommage produit ». |
| 2026-06-25 | @copywriter | `v3/docs/copy/page-publique-s10.md` | **Copy complet page publique trading.issa-capital.com pour primo-visiteur non-trader.** Nom de marque utilisé : Issa Capital (à réviser si Thomas valide Capvent ou autre nom @creative-strategy S10). Tagline reco "Le sens du marché. Mesuré." + hero "Savoir dans quel sens souffle le marché." + sous-titre + 3 étapes "Comment ça marche" + micro-explications LONG/SHORT/Win rate/Horizons + badge statut "En phase de mesure" (3 formulations) + libellés de menu humanisés + footer complet + disclaimer légal FR naturel. Zéro tiret cadratin, zéro P&L, win rate only, honnêteté totale sur le mode test. Handoff @fullstack pour intégration dans build_html.py. |
| 2026-06-02 | orchestration + @fullstack (×4) | **Améliorations bulletin : lisibilité + capteurs** | **Lisibilité** (`c00d702`) : synthèse des décisions EN HAUT, cellules allégées (`[pond]` affiché seulement si ≠ primaire, pondéré en tête sur cellules 📰), légende compacte (symboles présents only), actifs 🚫 regroupés — présentation only, mesure/conclusions inchangées. **Capteurs** : TIPS partagé S&P (`b5bba34`, +16% couverture pondérée) ; breadth S&P/Nasdaq câblé via **proxy EW/CW** RSP/SPY + QQQE/QQQ (`1637241`, zscore `signe:+1`, CAC laissé n/a faute d'ETF EW gratuit) ; Caixin laissé **n/a + news** (choix Thomas) ; **FedWatch : aucune source ZQ gratuite fiable** (Twelve/FRED/yfinance KO, CME 25$/mo) → **n/a (décidé : garder 0€, news couvre partiellement)**. Tests 642 verts. ⚠️ Calibration proxy breadth (`signe:+1`, hypothèse EW/CW↑=haussier) à valider dans la boucle prédiction→mesure en shadow. |

---

## Mémo de reprise — dernière session

- **Date/heure de clôture** : 2026-06-19 (~08h Paris)
- **Numéro de session** : **Session 8** (sur `main` — tout mergé et poussé). Prochaine = **Session 9**, à démarrer **depuis `main`**.
- **Résumé S8** : Session dense pilotée sur pièces, tout sur `main`, **1508 tests verts**. (1) **GO mesure L027** : la mesure des continus part désormais de l'**émission 7h** (fin du win rate gonflé — un short or perdant n'est plus classé « NC »). (2) **Briefing 7h refondu** (9 pts) + **« raison par cellule »** sur la Synthèse 24h/7j/1m (driver dominant net, biblio experte) **auditée 10/10 par les 3 experts** + note normalisée comparable inter-actifs. (3) **3 instrumentations SHADOW** (mesurer avant cutover) : persistance (le vrai « démenti » = le **quant qui se retourne**, L028), justesse news **par horizon**, détecteur **rumeur→verbal**. (4) **5 critères météo** rendus symétriques (sécheresse OU excès = haussier ; blé-AU gardé directionnel, verdict expert sourcé). (5) **Incident Juneteenth 19/06** : garde-fou « jour de bourse » trop large (NO-OP tout le cycle dès qu'UN marché US ferme) → corrigé (`is_any_market_open` + statut `marche-ferme`) ; rapport du jour **forcé** ; **bug parser news** réparé (**0→161** events à impact — Décor + News par actif étaient vides).
- **Travaux en cours / différés** :
  - **[3 cutovers measure-gated — décision sur ~2-3 sem. de shadow + GO Thomas]** : (a) **persistance** — faire vivre une news tant que le **quant ne la dément pas** (le quant garde le véto ; ~71% des structurels 7j/1m déjà démentis → « age-only » serait dangereux) ; (b) **rumeur→verbal** — activation **graduée** (reliability `rumor` strict d'abord, JAMAIS les ~1000 « reported » en masse) ; (c) **garde-fous démenti/déjà-coté** qui *baissent* la note (aujourd'hui flag-only). Instrumentation déjà posée (`persist_shadow_*`, `news_driven` persisté, `nature_shadow_downgrade`).
  - **[Question produit « rôle des news » — TRANCHÉE 20/06 (S9)]** : Thomas a **suivi la reco des 3 experts** → le **quant reste patron**, `NEWS_DOMINANT_RATIO` **inchangé** (on n'abaisse PAS pour donner plus de poids aux news). Piste retenue : rendre l'IA news plus maligne + activer les garde-fous démenti/déjà-coté, **mais après mesure** (les 3 instruments shadow). **Aucun changement de signal aujourd'hui** (cohérent « mesurer avant d'agir »). Contexte qui a motivé la question : la news ne dominait quasi aucun call (11 en 24h / 1 en 7j / 0 en 1m).
  - **[Ticket C ~23/06 — calibration sur données accumulées]** : contribution critère×horizon, seuil d'exécution des quasi-neutres (|score|<0,30), frein contre-momentum CAC, pertinence S&P 7j.
  - **[Chantiers data]** : **gel café** (Tmin<4°C non câblé), **clé USDA** (débloque blé WASDE), retirer le **cron temporaire sonde 8h** (verdict B rendu en S7).
- **Prochaines actions (priorisées)** :
  1. **[P1] Laisser les 3 instruments shadow accumuler ~2 sem.** puis trancher les cutovers sur les chiffres (@fullstack + @data-analyst). Ethos « mesurer avant d'agir ».
  2. ~~**[P1 — Thomas] Trancher la question produit « rôle des news »**~~ → **FAIT 20/06 (S9)** : quant reste dominant, `NEWS_DOMINANT_RATIO` inchangé (reco experts suivie).
  3. **[P1 ~23/06] Ticket C** (calibration accumulée).
  4. **[P2] Chantiers data** : gel café, clé USDA, retrait cron sonde 8h.
- **Blockers** :
  1. **Régénérer la clé Twelve Data** (action Thomas — elle a transité ; idéalement secret dédié).
  2. ~~Décision produit « rôle des news »~~ — **levé 20/06 (S9)** : tranché (quant dominant, ratio inchangé).
  3. Sources écartées (zéro invention) : secret `USDA_API_KEY` ; option `v3/data/manual/` (WGC or / ICE café / AAII).
- **Nom de branche recommandé prochaine session** : `claude/tradingapp-s9-mesure-cutovers-[suffix]`
- **Commande de reprise** :
  > « Reprise TradingApp **session 9, depuis `main`**. Lis project-context.md (Mémo S8 + Historique S8) + docs/founder-preferences.md (⚠️ WIN RATE ONLY ; validation shadow réel ; mesurer avant d'agir ; vérifier sur pièces avant d'affirmer ; itérer jusqu'à 10/10 avec les 3 experts). S8 a livré (tout sur `main`, 1508 tests) : GO mesure L027 (continus mesurés depuis l'émission 7h), refonte briefing 9 pts + **raison par cellule 10/10**, note normalisée, **3 instrumentations SHADOW** (persistance / news-par-horizon / rumeur), **5 critères météo symétriques** (blé-AU directionnel), et l'incident **Juneteenth** (garde-fou `is_any_market_open` + statut `marche-ferme` + **fix parser news 0→161 à impact**). **PRIORITÉ** : (1) laisser les 3 shadows accumuler ~2 sem. puis trancher les **cutovers sur preuve** — persistance (quant=véto), rumeur→verbal (graduée, rumor strict d'abord), garde-fous démenti/déjà-coté (sortir du flag-only) ; (2) **question produit Thomas** : le quant pilote ~tout le temps, la news ne domine quasi rien (11/1/0 calls news-driven 24h/7j/1m) → abaisser `NEWS_DOMINANT_RATIO` ou garder quant-dominant ? ; (3) Ticket C ~23/06. Rappels : tout changement de signal/mesure = cutover + addendum + ref_changed ; régénérer la clé Twelve ; vérifier la liste FAILED soi-même (L024). »

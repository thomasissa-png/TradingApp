<!-- Version: 2026-05-01T00:00 — @orchestrator — Création squelette v2 (projet neuf TradingApp) -->
# Lessons learned — TradingApp

> Format v2 obligatoire (11 colonnes). Cap dur : 80 lignes max (commandement n°8 CLAUDE.md).
> TTL : 5 sessions OU 90 jours (le plus court). Au-delà : promote en règle ou archive vers `lessons-archive.md`.
> P0 jamais archivés automatiquement (garde-fous silencieux).
> GATE BLOQUANTE en reprise : tout learning P0/P1 avec propagation = `non-propagé` doit être propagé AVANT tout nouveau travail.

| ID | Date | Phase | Agent | Sévérité | Description | Cible propagation | Fichiers impactés | Statut correction | Statut propagation | Notes |
|----|------|-------|-------|----------|-------------|-------------------|-------------------|-------------------|---------------------|-------|
| L001 | 2026-05-01 | 0 | @legal | P0 | PFU = 31,4 % depuis 01/01/2025 (12,8 % IR + 18,6 % PS), pas 30 %. Hausse effective des prélèvements sociaux. | Tout livrable mentionnant la fiscalité turbos / PFU / scénarios P&L net | project-context.md L45, docs/strategy/personas.md L191, docs/orchestration-plan.md L235 + L317 | ✅ corrigé | ✅ propagé (4 fichiers, vérifié grep "PFU 30") | Réf. Service-public.fr fiche F21618. |
| L002 | 2026-05-01 | 0 | @ia | P1 | Tag modèle Anthropic obligatoire : alias minor-family OK (`claude-sonnet-4-6` capte 4.6.x stable), tag exact daté OK (`claude-haiku-4-5-20251001`), JAMAIS d'alias cross-family `*-latest` / `*-newest` (peut basculer de génération sans warning = régression silencieuse). Évite la régression silencieuse en prod sur un signal qui engage du capital réel. | Tout code/livrable invoquant l'API Anthropic | docs/ia/ai-architecture.md §1.2, docs/ia/prompt-library.md §6 | ✅ corrigé (tag exact / minor-family alias retenus) | ✅ propagé (2 fichiers @ia) | Renforcement Phase 2c (@fullstack) : validator Pydantic `_no_alias_in_model_tag` refuse `*-latest/-newest/-current`. Couplé au champ `prompt_version` SQLite + au champ `model_used` JSON output. **Voir L010** : la règle alias ne suffit pas — il faut aussi que le tag pointe sur un modèle COURANT (pas déprécié). |
| L010 | 2026-05-02 | Post-Phase5 | @ia (escalade persona) | P0 | Tag modèle Anthropic obsolète : `claude-sonnet-4-5-20250929` était périmé — modèle actuel = Sonnet 4.6 (`claude-sonnet-4-6`) + Haiku 4.5 daté (`claude-haiku-4-5-20251001`). Correction massive 12+ fichiers post-livraison (config.py, conftest.py, 8 tests, 7 fixtures JSON, REPLIT_ACTIONS, 3 docs IA, project-context). | Tout code/livrable invoquant l'API Anthropic | src/config.py, src/ai/__init__.py, tests/conftest.py + 7 tests, tests/fixtures/ai/outputs/TC-0[1-4,6-8].json, docs/ia/ai-architecture.md v1.2, docs/ia/prompt-library.md, docs/ia/edge-scoring-model.md, docs/lessons-learned.md, REPLIT_ACTIONS.md, project-context.md | ✅ corrigé | ✅ propagé (audit grep complet — 0 occurrence `claude-sonnet-4-5` ou `claude-haiku-4-5` non daté hors L010 lui-même) | À renforcer : @ia DOIT vérifier la version courante du modèle Anthropic avant chaque session via system reminder ou WebSearch. Cohérent avec L002 mais ajoute "vérifier que le tag pointe sur le modèle actuel, pas un modèle déprécié". Pricing inchangé [HYPOTHÈSE — pricing 4.5 retenu pour estimation, à actualiser si Sonnet 4.6 différent]. |
| L003+L004 | 2026-05-01 | 0 | @infrastructure | P1 | **Coûts infra de référence (Phase 0)** : Replit Hacker (~7 €) discontinué → Replit Core 20 $/mois min (cron Deployment, pas Always-On) ; Twelve Data 1m intraday EU = plan Pro Individual 79 $/mois min (cache SQLite obligatoire anti-rate). | project-context.md, docs infra | ✅ corrigé | ✅ propagé | Fusion L003/L004 (hors TTL, faits stables) ; détail historique : git + CHANGELOG. |
| L007 | 2026-05-01 | 1 | @data-analyst + @qa (audit Phase 1b) | P1 | Critères GO Phase 2 durcis suite audit méthodologique @qa : 5→6 conditions AND, seuils Sharpe OOS 1,0→1,2 (Lo 2002 IC95%), robustesse 0,5→0,6 (Pardo 2008), walk-forward 2/3→3/3, drawdown annuel→mensuel (signal d'arrêt n°1 persona), nb_trades_OOS ≥ 50 ajouté, p-value paramétrée Politis-Romano stationary bootstrap n=10 000 + Hansen SPA (ou Bonferroni α=0,0071 fallback). | @testeur-backtest-edge lors de l'audit résultats H-X-results.json ; @fullstack pour paramétrage `arch` package Python | docs/analytics/edge-rnd-report.md v1.1, .claude/agents/testeur-backtest-edge.md (seuils synchronisés v1.1) | ✅ corrigé (edge-rnd-report.md v1.1 + testeur-backtest-edge.md v1.1) | ✅ propagé (5 Edits sur testeur-backtest-edge.md L43-L47, L111, L156, L190, L208 — vérifié grep : 0 résiduel "Sharpe.*1,0", "2/3", "≥ 0,5") | Probabilité PASS estimée ~55 % vs ~75 % v1.0 [HYPOTHÈSE]. `arch` Python package à valider en Phase 2 par @fullstack. |
| L012 | 2026-06-01 | shadow | orchestration | P1 | **GitHub Actions `schedule` = retardé 1-3h, pas fiable à l'heure pile, mais PAS en panne.** Ne JAMAIS déclarer un run cron « mort » avant ~3h de retard. Et NE PAS modifier le cron en réaction : changer le `cron:` re-enregistre le planning et fait sauter l'occurrence suivante (churn = aggravation). Diagnostic correct = onglet Actions → filtre Event:schedule. Parade : redondance ×3 par créneau + garde-fou anti-doublon (skip si snapshot <2h) ; fallback fiables = push fichier déclencheur ou workflow_dispatch ; pinger externe (cron-job.org → API dispatch) pour l'heure pile. | Tout projet à cron GitHub Actions critique | .github/workflows/cycle.yml, project-context.md État | ✅ corrigé (redondance + guard) | ✅ propagé (workflow + context) | Erreur de session : 43 min de retard interprétées comme panne → 3 changements de cron inutiles ayant probablement causé le raté de midi. |
| L013 | 2026-06-01 | shadow | @fullstack + 3 experts | P1 | **News LLM horizon-agnostique : ne jamais empiler un decay global par-dessus un mécanisme `pertinence` par critère existant.** DeepSeek sort 1 direction/actif sans notion d'horizon. La décroissance par horizon DOIT se régler via la `pertinence` par critère (qui peut décroître — choc géopol — OU monter — OPEC structurel à 1m). Un `decay_factor` uniforme {1.0/0.7/0.3} double-amortit ET écrase les drivers qui doivent monter (OPEC, EUDR) → contre-sens métier. Contre le cas pathologique « news inverse seule le quant » : cap anti-inversion `news_capped = min(\|news\|, \|quant\|×0.8)×signe`, override si materiality=high+reliability=confirmed. | Tout scoring multi-critères mêlant signal LLM + signal quant sur plusieurs horizons | v3/config/fiches/*.yml (pertinence), v3/scripts/scoring_analyste.py (cap) | ✅ corrigé (pertinence recalibrée + cap α=0.8) | ✅ propagé (3 fiches + scoring + 13 tests) | Cause racine non encore traitée (Phase 2) : distinguer news structurelle vs fraîche-déjà-price via event_id/date + flag nature. |

| L014 | 2026-06-02 | shadow | @fullstack + orchestration | P1 | **Un seuil de gate fixé sans données réelles peut rendre le système muet.** Le gate S5 (`COVERAGE_MIN=0.40`) a fait ressortir les 12 actifs en `INSUFFISANT` (couverture 0-35%) sur données réelles. Probable artefact de données vieilles (3 j), mais montre qu'un seuil de gate DOIT être validé/calibré sur un run de données FRAÎCHES avant d'être considéré comme actif. | agent-spécifique (@fullstack/@data-analyst) + documentation | v3/scripts/scoring_analyste.py (COVERAGE_MIN/OK), project-context.md (mémo) | ✅ corrigé (clos S9 20/06 : résolu dès S2 — l'incident FRED-429 a prouvé que `COVERAGE_MIN=0.40` est juste, cf. context L42 ; pas un seuil mal calibré) | ✅ propagé (documenté mémo + founder-prefs « mesurer avant d'agir ») | **Clos S9** (hors-TTL). La leçon est promue en règle durable « mesurer avant d'agir » (founder-preferences.md). |
| L015 | 2026-06-02 | shadow | @fullstack | P2 | **Pattern « gate flag-only en mode shadow »** : livrer un gate directionnel en DÉTECTION SEULE d'abord (trace + marqueur + mesure, SANS changer la conclusion), prouvé par test-verrou « conclusions inchangées ». Permet de mesurer la fréquence d'un problème AVANT de câbler l'action corrective et de calibrer ses seuils sur du réel. Appliqué Lots 4a/5/6. | règle-globale / documentation | docs/lessons-learned.md, _base-agent-protocol.md (si réutilisé 2e projet) | ✅ fait | ✅ propagé (appliqué 3 lots cette session) | À promouvoir en règle si réutilisé hors-trading. Différent d'un feature-flag : ici c'est un détecteur observable qui informe la calibration de l'action future. |
| L016 | 2026-06-02 | shadow | orchestration (3 experts) | P2 | **Pattern « concertation contradictoire + itération notée »** : pour une décision de conception à fort enjeu, faire produire 3 angles d'experts EN DÉBAT (chacun conteste/amende les autres) puis converger via rounds /10 jusqu'à plafond. Surperforme l'audit mono-agent (a fait passer la couverture requêtes news de ~58% à 10/10, et tranché 3 frictions de gates). | règle-globale / documentation | docs/founder-preferences.md, _base-agent-protocol.md | ✅ fait | ✅ propagé (founder-prefs + appliqué requêtes + gates) | Préférence fondateur confirmée (« fait auditer par nos 3 experts », « itère jusqu'à 10/10 »). |
| L017 | 2026-06-02 | shadow | orchestration | P1 | **Préférences fondateur Thomas capturées** (réponse directe à la question posée ; honnêteté brutale anti-spin ; refus des demi-traitements silencieux ; anti-sur-sollicitation/sur-cadrage ; « mesurer avant d'agir » ; concertation 3 experts ; objectif = justesse de tendance pas vitesse). | founder-prefs | docs/founder-preferences.md (créé), docs/lessons-learned.md | ✅ fait | ✅ propagé (founder-preferences.md créé — source de vérité cross-projets) | Détail ligne-à-ligne dans founder-preferences.md. À lire par l'orchestrator au démarrage de toute session. |
| L018 | 2026-06-09 | shadow | orchestration (Thomas a signalé) | P1 | **« Built but not surfaced » — un livrable backend invisible sur la vue utilisateur n'existe PAS pour l'utilisateur.** On a construit 5 nouveaux rapports + le tableau win-rate (fichiers .md), mais la PAGE (`build_html`) ne ramassait que `bulletin-*.md` → Thomas « ne voyait aucun rapport depuis ce matin ». Le reviewer l'avait noté (« build_html non touché ») mais pas traité comme bloquant. **Règle : toute nouvelle sortie destinée à l'utilisateur DOIT être branchée sur la vue qu'il consulte, dans la même phase/spec — la spec doit l'inclure comme critère d'acceptation, et le reviewer le traiter comme P0 si absent.** | règle-globale / documentation | docs/lessons-learned.md, .claude/agents/product-manager.md + reviewer.md (si réutilisé 2e projet) | ✅ corrigé (page branchée en 2 passes, 984 tests) | ✅ propagé (documenté ; à promouvoir en règle agent PM/reviewer) | L'utilisateur consomme la VUE rendue, pas l'arborescence de fichiers. Symptôme classique « ça marche en backend mais l'utilisateur ne le voit pas ». |
| L019 | 2026-06-09 | shadow | orchestration (Thomas ×N) | P1 | **WIN RATE ONLY — répété fortement par Thomas.** Le système trend-following se mesure UNIQUEMENT au taux de bonnes directions (oui/non). Le P&L / argent gagné / expectancy / amplitude-de-gain est **hors-sujet** et a agacé Thomas quand reproposé. L'amplitude % sert uniquement à seuiller VRAI/FAUX, jamais à afficher un montant. | founder-prefs | docs/founder-preferences.md, docs/lessons-learned.md | ✅ corrigé (vue win-rate-only, tests anti-mention monétaire) | ✅ propagé (founder-preferences.md mis à jour) | Corollaire : « validation = shadow réel uniquement, le backtest n'est pas un jalon » (Thomas 05/06, founder-prefs). |
| L020 | 2026-06-09 | shadow | @infrastructure (News Trader a rattrapé) | P1 | **Un cron dépendant d'une clôture/ouverture de marché DOIT se caler sur l'heure LOCALE réelle (DST-safe), jamais un UTC figé.** Le bilan du jour à `20h UTC` = 21h Paris en hiver → clôture US (22h Paris) ratée 5 mois/an. Et l'écart Paris↔NY change ~3 sem/an (DST désalignés) → jamais coder l'offset en dur. Parade éprouvée projet : **cron horaire + garde `datetime` en heure-Paris dans le script** (`ZoneInfo`). | agent-spécifique (@infrastructure) + règle-globale | .github/workflows/cycle.yml, v3/ops/vps-trigger/trigger-cycle.sh | ✅ corrigé (bilan calé 22h15 Paris réel) | ✅ propagé (code + cohérent L012 cron) | Étend L012 : pas seulement « schedule retardé » mais « heure de marché = heure locale réelle ». |
| L021 | 2026-06-09 | shadow | @fullstack + Thomas | P2 | **Mesurer un call directionnel depuis le prix d'ENTRÉE RÉEL (ouverture de marché), pas un prix de marché fermé.** L'ancien modèle mesurait le 24h depuis le prix de 7h (marché fermé, prix de nuit) → artefact (Or : FAUSSE alors que la session était VRAIE). Refonte : référence = ouverture propre de chaque marché, 24h jugé open→close (la nuit, marchés fermés → la journée EST le 24h). | agent-spécifique (@data-analyst/@fullstack) + documentation | v3/scripts/mesure_ouverture.py, v3/docs/reco/spec-refonte-5-rapports.md | ✅ corrigé (Phase 1 refonte) | ✅ propagé (code + spec) | Généralisable : aligner le point de référence de la mesure sur le point d'exécution réel. |
| L022 | 2026-06-09 | shadow | orchestration | P2 | **Pattern autopilote multi-phases qui surperforme pour un gros chantier** : cahier des charges (PM) → **audit trio contradictoire /10 → itération spec jusqu'à ~9,5** → build par phases avec dépendances → **revue finale croisée (@reviewer) GO/NO-GO** → merge. A livré la refonte « 5 rapports » (mesure + 5 runners + Manager + infra) proprement, 984 tests, 0 régression. | règle-globale / documentation | docs/lessons-learned.md, .claude/agents/orchestrator.md (si réutilisé) | ✅ fait | ✅ propagé (appliqué ; pattern documenté) | Préférence fondateur « itérer jusqu'à 10/10 » + audit avant code. Le reviewer en gate final évite les angles morts (cf. L018 qu'il avait vu mais sous-pondéré). |

| L023 | 2026-06-10 | shadow | orchestration (audit Cowork) | P1 | **Renommer un critère à mi-shadow casse l'agrégation par `nom` et induit de faux audits.** Un audit externe a cru voir 9 doublons (→ aurait « dédupliqué » 9 fiches SAINES) : c'étaient des critères **renommés les 7-8/06**, pas des doublons. Le checkpoint « audit livré + relu AVANT de toucher un YAML » a évité la casse. Règle : (a) tout renommage de critère passe par le CHANGELOG en citant sa `cle_courante` stable ; (b) les outils d'analyse (bilan, weekly, audits, contributions par critère) agrègent **TOUJOURS par `cle_courante`, JAMAIS par `nom`**. | Outils d'analyse par critère + tout futur audit de critères + @data-analyst | v3/scripts/journaliste.py + helpers d'agrégation par critère, .claude/agents/data-analyst.md (si réutilisé 2e projet) | ✅ fait (cutover v2 : (a) agrégation critères déjà par `cle_courante` partout — decision-log/scoring/triggers/criteres_calculator, vérifié ; (b) mapping ACTIF durci — `_winrate_rows` porte `fiche_key` stable, `render_weekly_winrate` ne round-trip plus par `nom`) | ✅ propagé | Étend le commandement 11 (grep ancien nom après renommage) au cas spécifique des critères de fiche. Le pattern « audit-avant-modif » est la vraie parade. |
| L024 | 2026-06-16 | shadow | orchestration | P1 | **Ne jamais se fier au « 22 env-only / pré-existant » d'un sous-agent — vérifier la liste FAILED fichier par fichier soi-même.** Des agents ont classé en « env-only » une vraie régression de gate (test_integration) ET un échec dû à pandas absent du conteneur (confondu avec un bug de remap). Un FAUX NÉGATIF a failli passer : « remap cassé » alors que fetch_history=None venait de pandas-absent + mon propre rate-limit API. Distinguer artefact d'env conteneur (pandas/holidays/clé/rate-limit) d'un vrai bug ; re-tester isolé avant de trancher. | règle-globale / documentation | docs/lessons-learned.md, orchestrator.md (si réutilisé) | ✅ fait | ✅ propagé (2 régressions rattrapées cette session) | Le résumé d'un agent n'est pas une preuve. |
| L025 | 2026-06-16 | shadow | orchestration | P1 | **Vérifier sur pièces AVANT d'affirmer ; concéder vite quand la donnée contredit.** 2 explications fausses données de mémoire (look-ahead, « autre horizon »), rejetées par Thomas ; seul l'accès aux pièces (decision-log, events-log, API Twelve réelle) a révélé la vérité (bug fenêtre de mesure). | founder-prefs / règle-globale | docs/founder-preferences.md, docs/lessons-learned.md | ✅ fait | ✅ propagé (founder-prefs) | Thomas exige des réponses adossées aux données, pas théoriques. Étend L017. |
| L026 | 2026-06-16 | shadow | @fullstack | P1 | **La source DÉCLARÉE d'un critère doit être la source RÉELLE, et la provenance doit être loggée.** Twelve ne sert PAS l'historique des futures (GC=F/SI=F/BZ=F=404) → dépendance à un fallback yfinance CACHÉ alors que les fiches disaient « Twelve ». Fix : symboles natifs (or→XAU/USD, argent→XAG/USD, Brent→XBR/USD), libellés honnêtes, provenance (twelve_native/yfinance/stooq) loggée. | agent-spécifique (@fullstack/@infra) + documentation | v3/scripts/market_data.py, criteres_calculator.py, config/fiches/*.yml | ✅ fait (or/argent/Brent) | ✅ propagé (libellés honnêtes + provenance loggée sur les 12 ; cuivre/cacao/café/blé restent yfinance car Twelve ne les sert PAS = chantier DATA séparé, **pas une propagation de code manquante** — ne bloque pas le gate de reprise) | Un fallback silencieux masque une dépendance fragile. |
| L027 | 2026-06-16 | shadow | orchestration (Thomas) | P0 | **Mesure d'un call sur actif CONTINU : une « ouverture » stampée à HEURE FIXE (8h Paris) peut tomber au milieu d'un mouvement → un call perdant classé « NC » → CACHE DES PERTES, FLATTE le win rate.** Confirmé via XAU/USD : 15/06 or open 4215→close 4309 (+2,2 %), short perdant, classé NC (+0,18 %) car réf 8h=4308. Fix retenu (GO mesure 16/06) : continus mesurés depuis l'ÉMISSION 7h (point d'exécution réel, L021) ; option bougie open→close écartée (open de bougie = frontière de session arbitraire ≠ 7h quand Thomas agit → recrée le bug). Non-continus inchangés (ouverture marché). | agent-spécifique (@data-analyst/@fullstack) + règle-globale | v3/scripts/journaliste.py (`_resolve_prix_reference` group-aware), scoring_analyste.py (étiquette dégradée group-aware), data/ref-changed.json | ✅ corrigé (fix + cutover 8 continus au 17/06 + addendums KILL/SELECTION + CHANGELOG) | ✅ propagé (test-verrou `test_verrou_l027_mesure_continu.py` : or 15/06 SHORT = FAUX, plus NC ; 1323 tests verts) | Étend L021 : la fenêtre de mesure ne doit pas tronquer le mouvement jugé. WR = KPI unique → mesure exacte impérative. 4e reset des continus, vrai défaut. |
| L028 | 2026-06-18 | shadow | orchestration (3 experts + mesure) | P1 | **Expiration d'un signal NEWS = désaveu par le QUANT, pas un démenti textuel — la plupart des news meurent en silence.** Sur la question « une tendance reste valide tant que non démentie », les 3 experts (Spéculateur/Newstrader/Analyst) + la mesure convergent : le vrai démenti est le quant qui se retourne (détecteur `DENIAL_KEYWORDS` = **0 déclenchement** sur tout l'historique). Sur les structurels 7j/1m, le quant dément déjà la news **~71 %** du temps → persistance « par âge seul » DANGEREUSE ; le quant garde le véto. Instrumenté en SHADOW (champs `persist_shadow_*` additifs, zéro impact signal) AVANT tout cutover. | scoring/news + @fullstack/@data-analyst + founder-prefs | v3/scripts/triggers_classifier.py (COEF_NATURE/already_priced/denial), scoring_analyste.py (champs shadow), v3/audit/persistance-shadow-mesure.md | ✅ instrumenté shadow + mesuré | ⏳ cutover NON-fait (attend accumulation forward >30j + GO Thomas sur les chiffres) | Étend L013/L015 : mesurer avant câbler. Le « démenti » est quantitatif, pas textuel. Backstop d'âge = filet, pas couperet. |

| L029 | 2026-06-19 | shadow | orchestration + @fullstack | P1 | **Un parser ancré « en fin de ligne » casse EN SILENCE quand le schéma de données gagne des colonnes en queue.** Le parser d'affichage news du briefing (regex `…\|$` supposant materiality\|reliability en 2 dernières colonnes) droppait toutes les lignes events-log au format courant (19 cols : +event_id/canonical_date/nature) → « 0 à impact / aucune actualité » partout (Décor + News par actif) alors que **161 events high/medium** existaient. Le scoring (parser SÉPARÉ `triggers_classifier`, par index nommé) n'avait PAS le bug. | agent-spécifique (@fullstack) + règle-globale | v3/scripts/briefing.py | fait | propagé (fix + 6 tests, 0→161 vérifié) | **Prévention : parser par INDEX de colonne nommée, jamais ancré en fin de ligne ; robuste à tout ajout de colonne futur.** Symptôme « tout vide » = souvent un parser désync du schéma, pas un manque de données. |
| L030 | 2026-06-19 | shadow | orchestration + @fullstack | P1 | **Un garde-fou « jour de bourse » binaire NO-OP tout le pipeline dès qu'UN marché ferme — alors qu'un univers multi-marchés a des actifs qui tradent.** Le 19/06 (Juneteenth, NYSE fermé) a blanchi tout le briefing alors que CAC + 8 continus cotaient (~8 fériés US/an concernés). Fix : `is_any_market_open(d)` (tourne si ≥1 marché ouvert) + statut `marche-ferme` par actif (les fermés non jugés → zéro mesure 24h dégénérée, exclus KPI). | agent-spécifique (@infra/@fullstack) + règle-globale | v3/scripts/journaliste.py, .github/workflows/cycle.yml | fait | propagé (fix + 10 tests Juneteenth) | **Prévention : univers multi-marchés ⇒ garde + mesure PAR-MARCHÉ, jamais all-or-nothing.** Étend la garde fériés de S3 (06/06). |
| L031 | 2026-06-19 | shadow | orchestration | P1 | **Préférences fondateur S8** : (1) « **corrige, ne log pas** » (réparer un bug découvert tout de suite ; exception : un changement de SIGNAL se mesure d'abord en shadow) ; (2) **délègue les arbitrages de fond aux 3 experts** et veut leur verdict APPLIQUÉ, pas un menu ; (3) **page/rapports ÉPURÉS** (efficacité/pertinence > exhaustivité à l'écran). | founder-prefs | docs/founder-preferences.md | fait | propagé (copiées dans founder-preferences.md) | Détail ligne-à-ligne dans founder-preferences.md (source cross-projets). |
| L032 | 2026-06-21 | S9 | orchestration | P0 | **« Les 3 experts » = le TRIO MÉTIER de `v3/audit/README.md`, JAMAIS des agents génériques (elon/reviewer/data-analyst).** Erreur répétée chaque session. Quand Thomas dit « nos 3 experts / le trio / le panel » : **Analyst** (rigueur stat : échantillon, Wilson, Brier, indépendance), **News Trader** senior 15 ans desk macro/commodities (justesse directionnelle, défendabilité desk, cohérence news→critère→score), **Spéculateur** trend-follower (« est-ce que je mets du cash dessus ? » : conviction par cellule, mono-critère trompeur, coin-flips, edge exploitable). Les incarner avec LEUR posture exacte. | Toute demande Thomas « 3 experts / trio / panel » | v3/audit/README.md (vérité), docs/lessons-learned.md | ✅ corrigé (incarnation des 3 postures) | ✅ propagé (P0) | Rendus = `v3/audit/<étape>-<expert>.md`. Lire README AVANT de lancer le panel. |

## Agents fragiles (circuit breaker)

| Agent | Type d'échec | Fréquence | Dernière occurrence | Contournement |
|-------|--------------|-----------|---------------------|---------------|

## Notes

- Projet TradingApp démarré le 2026-05-01.
- Aucun learning hérité au démarrage. Tableau vide attendu jusqu'à fin de Phase 0.
- Les learnings cross-projets de référence (zéro fausse promesse copy, anti-témoignage fictif, conviction-first, anti-placeholder galerie, backoffice = même DS, self-fetch Next.js 127.0.0.1:PORT, hooks AVANT return conditionnel, stale-while-revalidate fetch >3s, seuil réécriture 10 edits, migrations SQL idempotentes, flux progressifs IA, protocole migration modèle IA, parcours d'achat e2e, bugs corrigés immédiats, agents testeurs calibrés VALEUR, @sales-enablement obligatoire en Phase 4, earned media Phase 4 @growth, pre-commit build check, Actions Replit, favicons/icônes) sont assumés intégrés dans CLAUDE.md / `_base-agent-protocol.md` / les agents — pas re-listés ici pour respecter le cap 80L.

# TradingApp v3 — dependencies (Phase 2.1, GitHub Actions runtime)
# Python 3.11+

# HTTP + RSS
requests==2.31.0
feedparser==6.0.10

# Market data : Twelve Data (primary) via requests + yfinance (fallback indices)
# yfinance est obligatoire pour les indices (^GSPC, ^IXIC, ^FCHI, ^VIX, ^RUT...)
# qui ne sont pas servis par le plan Twelve Grow.
yfinance==0.2.40
pandas==2.2.2

# Parsing HTML (utilisé par feedparser pour certains feeds malformés)
beautifulsoup4==4.12.2
lxml==5.0.0

# Configuration
pyyaml==6.0.1
python-dotenv==1.0.0

# LLM extraction — OpenAI SDK compatible avec DeepSeek
openai==1.55.0
# httpx épinglé < 0.28 : openai 1.55 passe `proxies=` à httpx, retiré en 0.28+
# (sinon TypeError: Client.__init__() got an unexpected keyword argument 'proxies')
httpx==0.27.2

# Logging
structlog==24.1.0

# Phase 2 — dédup floue par Levenshtein normalisée (event_id, anti-repost).
# Si absent à l'exécution, triggers_classifier bascule en mode dégradé
# (SHA exact seul) — le code ne crash pas.
rapidfuzz==3.9.6

# Fériés de marché AUTO (NYSE XNYS + Euronext XECB), calculés pour toute année
# → fin de la maintenance annuelle manuelle de MARKET_HOLIDAYS. Pure-python,
# zéro dépendance binaire. Si absent à l'exécution, `_is_market_holiday`
# retombe proprement sur le socle statique MARKET_HOLIDAYS (jamais de crash).
holidays==0.98

# NB: Drive API supprimé (remplacé par git-as-storage)
# NB: schedule supprimé (remplacé par GitHub Actions cron)
# NB: sqlite intégré à la stdlib Python — pas besoin de sqlite-utils en prod
